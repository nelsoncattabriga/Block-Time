---
phase: 02-coredata-repository
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - Block-Time/Block_TimeApp.swift
  - Block-Time/Views/Screens/SplashScreenView.swift
  - Block-Time/Models/SchemaV1.swift
  - Block-Time/Models/FlightModel.swift
  - Block-Time/Models/AircraftModel.swift
  - Block-Time/Infrastructure/ModelContainerFactory.swift
  - Block-Time/Migration/CoreDataMigrationService.swift
  - Block-Time/Migration/CoreDataMigrationActor.swift
  - Block-Time/Migration/LegacyFlightSnapshot.swift
  - Block-Time/Repositories/SwiftDataFlightRepository.swift
files_read_only:
  - Block-Time/Infrastructure/AppRepositoryEnvironment.swift
autonomous: false
requirements: [REPO-07, REPO-08]
must_haves:
  truths:
    - "No SwiftData imports remain anywhere in the Block-Time app target"
    - "Block_TimeApp injects CoreDataFlightRepository at top level via .flightRepository(...)"
    - "All SwiftData-only files deleted (SchemaV1, FlightModel, AircraftModel, ModelContainerFactory, CoreDataMigrationService, CoreDataMigrationActor, LegacyFlightSnapshot, SwiftDataFlightRepository)"
    - "SplashScreenView no longer triggers the SwiftData migration task"
    - "Orphan blocktime.sqlite (Phase 1 SwiftData store) deleted from App Group on first launch if present"
    - "UserDefaults v2MigrationStarted and v2MigrationComplete flags cleared on first launch"
    - "AppRepositoryEnvironment default stays InMemoryFlightRepository (previews still work without CloudKit) — verified read-only, not modified"
  artifacts:
    - path: "Block-Time/Block_TimeApp.swift"
      provides: "App entry with CoreDataFlightRepository injection and orphan cleanup"
      contains: "CoreDataFlightRepository"
    - path: "Block-Time/Views/Screens/SplashScreenView.swift"
      provides: "Splash screen with migration task removed"
      contains: "SplashScreenView"
    - path: "Block-Time/Infrastructure/AppRepositoryEnvironment.swift"
      provides: "Environment key for FlightRepository, default InMemoryFlightRepository (verified unchanged)"
      contains: "FlightRepositoryKey"
  key_links:
    - from: "Block_TimeApp.swift"
      to: "CoreDataFlightRepository + FlightDatabaseService.shared.persistentContainer"
      via: ".flightRepository(CoreDataFlightRepository(container: ...))"
      pattern: "CoreDataFlightRepository\\(container:"
    - from: "Block_TimeApp.init()"
      to: "App Group container blocktime.sqlite"
      via: "FileManager.default.containerURL + removeItem if exists"
      pattern: "blocktime.sqlite"
---

<objective>
Delete all SwiftData infrastructure from the Block-Time app target, rewire `Block_TimeApp` to inject `CoreDataFlightRepository`, remove the SwiftData migration trigger from `SplashScreenView`, and add one-shot orphan cleanup for the Phase 1 SwiftData store file plus stale UserDefaults flags. Then verify (read-only) that `AppRepositoryEnvironment` already conforms to D-25 — this is a sanity check, not a modification.

Purpose: REPO-07 (SwiftData infrastructure deleted) and REPO-08 (app entry injects CoreDataFlightRepository). Depends on Plan 03 (`CoreDataFlightRepository` must exist before we wire it). After this plan, the app builds and launches with zero SwiftData references.

Output: 8 SwiftData files deleted, `Block_TimeApp.swift` and `SplashScreenView.swift` updated, `AppRepositoryEnvironment.swift` verified unchanged (or surgically corrected only if it accidentally references deleted SwiftData types), project builds and launches.

**This plan contains a CHECKPOINT for human verification of first-launch migration behaviour on simulator (cannot be automated — requires observing the running app).**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-coredata-repository/02-CONTEXT.md
@.planning/phases/02-coredata-repository/02-RESEARCH.md
@.planning/phases/02-coredata-repository/02-03-SUMMARY.md

@Block-Time/Block_TimeApp.swift
@Block-Time/Views/Screens/SplashScreenView.swift
@Block-Time/Infrastructure/AppRepositoryEnvironment.swift
@Block-Time/Infrastructure/ModelContainerFactory.swift
@Block-Time/Services/FlightDatabaseService.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete all SwiftData infrastructure files and remove SwiftData imports</name>
  <files>
    Block-Time/Models/SchemaV1.swift,
    Block-Time/Models/FlightModel.swift,
    Block-Time/Models/AircraftModel.swift,
    Block-Time/Infrastructure/ModelContainerFactory.swift,
    Block-Time/Migration/CoreDataMigrationService.swift,
    Block-Time/Migration/CoreDataMigrationActor.swift,
    Block-Time/Migration/LegacyFlightSnapshot.swift,
    Block-Time/Repositories/SwiftDataFlightRepository.swift
  </files>
  <read_first>
    - Block-Time/Models/SchemaV1.swift (verify nothing else in the app target depends on it before deletion)
    - Block-Time/Models/FlightModel.swift (same)
    - Block-Time/Models/AircraftModel.swift (same)
    - Block-Time/Infrastructure/ModelContainerFactory.swift (read appGroupStoreURL() — orphan cleanup in Task 2 must inline the URL construction since this file is being deleted)
    - Block-Time/Migration/CoreDataMigrationService.swift (note the v2MigrationStarted / v2MigrationComplete UserDefaults keys — Task 2 must clear these)
    - Block-Time/Migration/CoreDataMigrationActor.swift
    - Block-Time/Migration/LegacyFlightSnapshot.swift
    - Block-Time/Repositories/SwiftDataFlightRepository.swift (template was already absorbed into CoreDataFlightRepository in Plan 03)
    - .planning/phases/02-coredata-repository/02-CONTEXT.md (D-02, D-24)
  </read_first>
  <action>
    DELETE these 8 files using `rm` (folder-based Xcode inclusion means the project automatically stops compiling them):

    ```
    rm Block-Time/Models/SchemaV1.swift
    rm Block-Time/Models/FlightModel.swift
    rm Block-Time/Models/AircraftModel.swift
    rm Block-Time/Infrastructure/ModelContainerFactory.swift
    rm Block-Time/Migration/CoreDataMigrationService.swift
    rm Block-Time/Migration/CoreDataMigrationActor.swift
    rm Block-Time/Migration/LegacyFlightSnapshot.swift
    rm Block-Time/Repositories/SwiftDataFlightRepository.swift
    ```

    DO NOT delete (per CONTEXT.md):
    - `Block-Time/Migration/MigrationError.swift` — may be used elsewhere
    - `Block-Time/Migration/TimeStringConverter.swift` — Phase 3 will use it for the calculator
    - `Block-Time/Models/AircraftEntity.swift` if it exists — that is the Core Data entity, NOT the SwiftData model

    After deletion, do a project-wide sweep for any remaining `import SwiftData`:

    ```
    grep -rn "import SwiftData" Block-Time/
    ```

    Expected remaining references after deletion (all in files modified by later tasks in this plan):
    - `Block-Time/Block_TimeApp.swift` — line ~9 (will be removed in Task 2)

    Any other remaining `import SwiftData` reference is a bug — find the file and remove the import. If the file uses SwiftData types (e.g., `@Model`, `ModelContainer`, `ModelContext`), those usages must also be removed (D-24).

    DO NOT modify `Block_TimeApp.swift` or `SplashScreenView.swift` in this task — Tasks 2/3 handle them.
  </action>
  <verify>
    <automated>test ! -f /Users/nelson/Library/CloudStorage/OneDrive-Personal/Coding/Xcode/Block-Time/Block-Time/Models/SchemaV1.swift && test ! -f /Users/nelson/Library/CloudStorage/OneDrive-Personal/Coding/Xcode/Block-Time/Block-Time/Repositories/SwiftDataFlightRepository.swift && test ! -f /Users/nelson/Library/CloudStorage/OneDrive-Personal/Coding/Xcode/Block-Time/Block-Time/Migration/CoreDataMigrationService.swift && echo "deleted"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f Block-Time/Models/SchemaV1.swift` exits 0
    - `test ! -f Block-Time/Models/FlightModel.swift` exits 0
    - `test ! -f Block-Time/Models/AircraftModel.swift` exits 0
    - `test ! -f Block-Time/Infrastructure/ModelContainerFactory.swift` exits 0
    - `test ! -f Block-Time/Migration/CoreDataMigrationService.swift` exits 0
    - `test ! -f Block-Time/Migration/CoreDataMigrationActor.swift` exits 0
    - `test ! -f Block-Time/Migration/LegacyFlightSnapshot.swift` exits 0
    - `test ! -f Block-Time/Repositories/SwiftDataFlightRepository.swift` exits 0
    - `test -f Block-Time/Migration/MigrationError.swift` exits 0 (NOT deleted)
    - `test -f Block-Time/Migration/TimeStringConverter.swift` exits 0 (NOT deleted)
    - `grep -rln "import SwiftData" Block-Time/ | grep -v Block_TimeApp.swift | wc -l` returns 0 (only Block_TimeApp may still reference; will be cleaned in Task 2)
  </acceptance_criteria>
  <done>All 8 SwiftData files deleted; MigrationError + TimeStringConverter preserved; only remaining SwiftData import is in Block_TimeApp.swift (cleaned in next task).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewire Block_TimeApp — inject CoreDataFlightRepository, remove SwiftData, add orphan cleanup</name>
  <files>Block-Time/Block_TimeApp.swift</files>
  <read_first>
    - Block-Time/Block_TimeApp.swift (CURRENT — locate `productionContainer` static, `OptionalModelContainerModifier`, every `.modelContainer(...)` call, `import SwiftData`, and the existing `init()` if present)
    - Block-Time/Services/FlightDatabaseService.swift (read enough to confirm `FlightDatabaseService.shared.persistentContainer` is the public reference and is an `NSPersistentCloudKitContainer`)
    - Block-Time/Infrastructure/AppRepositoryEnvironment.swift (CURRENT — to confirm the `.flightRepository(_:)` modifier or `FlightRepositoryKey.defaultValue` name; we set the production repo via this)
    - .planning/phases/02-coredata-repository/02-CONTEXT.md (D-03, D-04, D-22, D-23, D-24)
    - .planning/phases/02-coredata-repository/02-RESEARCH.md (Pitfall 6 — orphan path must be inlined since ModelContainerFactory is deleted)
  </read_first>
  <action>
    Modify `Block-Time/Block_TimeApp.swift`:

    1. **Remove `import SwiftData`** (line ~9). The file must have zero SwiftData references after this task.

    2. **Delete `productionContainer` static property** if present (the static var/let that constructs a ModelContainer).

    3. **Delete `OptionalModelContainerModifier`** if defined in this file (a `ViewModifier` struct that conditionally applies `.modelContainer`).

    4. **Delete every `.modelContainer(...)` call** in the body / view modifier chain.

    5. **Keep `.environment(\.managedObjectContext, FlightDatabaseService.shared.viewContext)`** — this stays (D-22).

    6. **Add CoreDataFlightRepository injection at the top of the View hierarchy** (the `WindowGroup`'s root view) using whatever modifier `AppRepositoryEnvironment.swift` exposes. The expected form (per D-22):

       ```swift
       .flightRepository(CoreDataFlightRepository(container: FlightDatabaseService.shared.persistentContainer))
       ```

       If the modifier name differs in `AppRepositoryEnvironment.swift`, use the actual modifier name — the contract is the injection of a `CoreDataFlightRepository` instance into the FlightRepository environment slot at the top-level WindowGroup.

    7. **Add `init()` block to the App struct** (or extend the existing one if present) with orphan cleanup. The full init body must contain (D-03, D-04 + Pitfall 6):

       ```swift
       init() {
           let cleanupKey = "phase2OrphanCleanupDone"
           if !UserDefaults.standard.bool(forKey: cleanupKey) {
               // D-03: delete orphaned SwiftData store from Phase 1 (App Group only — dev builds only)
               if let appGroupURL = FileManager.default.containerURL(
                   forSecurityApplicationGroupIdentifier: "group.com.thezoolab.blocktime"
               ) {
                   let storeURL = appGroupURL.appendingPathComponent("blocktime.sqlite")
                   for suffix in ["", "-shm", "-wal"] {
                       let url = appGroupURL.appendingPathComponent("blocktime.sqlite" + suffix)
                       try? FileManager.default.removeItem(at: url)
                   }
                   _ = storeURL  // silence unused warning if the var is otherwise unused
               }
               // D-04: clear stale Phase 1 migration flags
               UserDefaults.standard.removeObject(forKey: "v2MigrationStarted")
               UserDefaults.standard.removeObject(forKey: "v2MigrationComplete")
               UserDefaults.standard.set(true, forKey: cleanupKey)
           }
       }
       ```

       Verify the App Group identifier `group.com.thezoolab.blocktime` matches the actual identifier in the project's entitlements. If it differs (check `Block-Time/Block-Time.entitlements` for `com.apple.security.application-groups`), use the correct identifier.

       If the App struct already has an `init()`, ADD the cleanup block at the top of it (preserve all existing behaviour per project rule).

    8. **Verify** — after editing:
       - No `import SwiftData` in the file
       - No `productionContainer`, no `OptionalModelContainerModifier`, no `.modelContainer(`
       - `CoreDataFlightRepository(container: FlightDatabaseService.shared.persistentContainer)` appears once
       - The `init()` has the orphan-cleanup block guarded by the `phase2OrphanCleanupDone` UserDefaults flag
       - File still compiles

    DO NOT remove `FlightDatabaseService` initialisation, theme injection, or any other unrelated wiring. ONLY remove SwiftData wiring + add Core Data repo injection + add orphan cleanup.
  </action>
  <verify>
    <automated>cd /Users/nelson/Library/CloudStorage/OneDrive-Personal/Coding/Xcode/Block-Time && xcodebuild -scheme Block-Time -destination 'generic/platform=iOS' -quiet build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import SwiftData" Block-Time/Block_TimeApp.swift` returns 0
    - `grep -c "productionContainer" Block-Time/Block_TimeApp.swift` returns 0
    - `grep -c "OptionalModelContainerModifier" Block-Time/Block_TimeApp.swift` returns 0
    - `grep -c "\.modelContainer(" Block-Time/Block_TimeApp.swift` returns 0
    - `grep -c "CoreDataFlightRepository(container:" Block-Time/Block_TimeApp.swift` returns 1
    - `grep -c "FlightDatabaseService.shared.persistentContainer" Block-Time/Block_TimeApp.swift` returns at least 1
    - `grep -c "\.environment(\\\\.managedObjectContext" Block-Time/Block_TimeApp.swift` returns at least 1 (existing wiring preserved)
    - `grep -c "phase2OrphanCleanupDone" Block-Time/Block_TimeApp.swift` returns at least 2 (read + write)
    - `grep -c "blocktime.sqlite" Block-Time/Block_TimeApp.swift` returns at least 1
    - `grep -c "v2MigrationStarted\|v2MigrationComplete" Block-Time/Block_TimeApp.swift` returns at least 2
    - `xcodebuild -scheme Block-Time -destination 'generic/platform=iOS' build` exits 0
    - `grep -rln "import SwiftData" Block-Time/ | wc -l` returns 0 (project-wide sweep)
  </acceptance_criteria>
  <done>Block_TimeApp.swift contains zero SwiftData references, injects CoreDataFlightRepository, retains managedObjectContext injection, has guarded one-shot orphan cleanup block.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove SwiftData migration trigger from SplashScreenView</name>
  <files>Block-Time/Views/Screens/SplashScreenView.swift</files>
  <read_first>
    - Block-Time/Views/Screens/SplashScreenView.swift (CURRENT — locate the `.task { ... }` block that invokes `CoreDataMigrationService` or similar)
    - .planning/phases/02-coredata-repository/02-CONTEXT.md (D-02)
  </read_first>
  <action>
    Modify `Block-Time/Views/Screens/SplashScreenView.swift`:

    1. Locate the `.task { ... }` modifier (or `.onAppear { ... }` with async Task) that calls into `CoreDataMigrationService`, `CoreDataMigrationActor`, or any of the deleted migration types.
    2. DELETE the entire `.task { ... }` block (or the specific call to `CoreDataMigrationService.runMigration(...)` etc.).
    3. PRESERVE all other splash logic (animation, version display, navigation-to-main-app trigger, etc.). The splash must still function as a splash — only the migration call goes away.
    4. Remove `import SwiftData` if it appears in this file.

    After editing, no reference to any of these deleted types should remain:
    - `CoreDataMigrationService`
    - `CoreDataMigrationActor`
    - `LegacyFlightSnapshot`
    - `ModelContainerFactory`
    - `SchemaV1`
    - `FlightModel`
    - `AircraftModel`
    - `SwiftDataFlightRepository`

    File must still compile.
  </action>
  <verify>
    <automated>cd /Users/nelson/Library/CloudStorage/OneDrive-Personal/Coding/Xcode/Block-Time && xcodebuild -scheme Block-Time -destination 'generic/platform=iOS' -quiet build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CoreDataMigrationService\|CoreDataMigrationActor\|LegacyFlightSnapshot\|ModelContainerFactory\|SchemaV1\|FlightModel\|AircraftModel\|SwiftDataFlightRepository" Block-Time/Views/Screens/SplashScreenView.swift` returns 0
    - `grep -c "import SwiftData" Block-Time/Views/Screens/SplashScreenView.swift` returns 0
    - `grep -c "SplashScreenView\|var body" Block-Time/Views/Screens/SplashScreenView.swift` returns at least 2 (splash struct still intact)
    - `xcodebuild -scheme Block-Time -destination 'generic/platform=iOS' build` exits 0
  </acceptance_criteria>
  <done>SplashScreenView has no migration call and no SwiftData references; splash UI logic preserved; build succeeds.</done>
</task>

<task type="verify" tdd="false">
  <name>Task 4: Verify (read-only) AppRepositoryEnvironment default stays InMemoryFlightRepository</name>
  <files>Block-Time/Infrastructure/AppRepositoryEnvironment.swift</files>
  <read_first>
    - Block-Time/Infrastructure/AppRepositoryEnvironment.swift (CURRENT — check the `defaultValue` of FlightRepositoryKey)
    - .planning/phases/02-coredata-repository/02-CONTEXT.md (D-25)
  </read_first>
  <action>
    READ-ONLY SANITY CHECK — this task does NOT modify the file under normal conditions. Run the acceptance-criteria grep commands. If all pass with the file untouched, the task is complete and no edits are needed.

    Only IF a grep check fails (e.g., the file has a stale `import SwiftData` from earlier work, or references a now-deleted type like `SwiftDataFlightRepository`), make the MINIMAL correction:

    1. Verify `FlightRepositoryKey.defaultValue` is `InMemoryFlightRepository()` (or equivalent). DO NOT change it — D-25 mandates it stays as the preview default.
    2. If a stray `import SwiftData` exists, remove ONLY that import line.
    3. If a reference to `SwiftDataFlightRepository` exists, replace with `InMemoryFlightRepository` only if the reference was the default value; otherwise flag as a deeper wiring issue.
    4. DO NOT add `productionContainer` or `ModelContainer` wiring — the environment key file knows only about `FlightRepository`; production wiring lives in `Block_TimeApp`.

    If no corrections needed, this task is a pure verification — pass the acceptance criteria and move on.
  </action>
  <verify>
    <automated>grep -c "InMemoryFlightRepository" /Users/nelson/Library/CloudStorage/OneDrive-Personal/Coding/Xcode/Block-Time/Block-Time/Infrastructure/AppRepositoryEnvironment.swift && test "$(grep -c "import SwiftData" /Users/nelson/Library/CloudStorage/OneDrive-Personal/Coding/Xcode/Block-Time/Block-Time/Infrastructure/AppRepositoryEnvironment.swift)" -eq 0 && test "$(grep -c "SwiftDataFlightRepository\|ModelContainer\|SchemaV1\|FlightModel" /Users/nelson/Library/CloudStorage/OneDrive-Personal/Coding/Xcode/Block-Time/Block-Time/Infrastructure/AppRepositoryEnvironment.swift)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "InMemoryFlightRepository" Block-Time/Infrastructure/AppRepositoryEnvironment.swift` returns at least 1
    - `grep -c "import SwiftData" Block-Time/Infrastructure/AppRepositoryEnvironment.swift` returns 0
    - `grep -c "SwiftDataFlightRepository\|ModelContainer\|SchemaV1\|FlightModel" Block-Time/Infrastructure/AppRepositoryEnvironment.swift` returns 0
    - `grep -c "FlightRepositoryKey" Block-Time/Infrastructure/AppRepositoryEnvironment.swift` returns at least 1
  </acceptance_criteria>
  <done>AppRepositoryEnvironment.swift verified: defaultValue is InMemoryFlightRepository, no SwiftData imports or references. File modified ONLY if a check failed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Verify first-launch migration behaviour on simulator</name>
  <files>Block-Time/Block_TimeApp.swift, Block-Time/Views/Screens/SplashScreenView.swift</files>
  <action>Human-only verification — see <how-to-verify> below for full instructions. Build, launch, observe migration behaviour on simulator. Cannot be automated because it requires inspecting running-app state and simulator container contents.</action>
  <verify>See <how-to-verify> below and the <acceptance_criteria> grep checks.</verify>
  <what-built>
    Phase 2 wiring is complete:
    - 8 SwiftData files deleted, no `import SwiftData` anywhere
    - `Block_TimeApp` injects `CoreDataFlightRepository` and runs one-shot orphan cleanup on first launch
    - `FlightDatabaseService` has migration options set; V2 model + custom mapping model + `FlightEntityMigrationPolicy` handle the V1→V2 store migration automatically
    - `SplashScreenView` no longer triggers the old SwiftData migration

    This checkpoint verifies the END-TO-END behaviour by running the app on simulator with existing v1 data (a real device test for REPO-10 CloudKit sync survival is deferred to Phase 8 per CONTEXT.md, but on-simulator migration correctness is checked here).
  </what-built>
  <how-to-verify>
    **Step A — Build on simulator**

    1. In Xcode, select an iPhone simulator destination (iPhone 16 Pro or similar).
    2. Build & Run (⌘R). The app must launch without crashing.
    3. Confirm in Xcode console: NO Core Data error like "Can't find mapping model for migration" or "entity migration policy class not found".

    **Step B — Verify simulator-seeded flights migrated correctly**

    The project has simulator flight seeding logic (per CLAUDE.md memory: `SplashScreenView.onAppear` + simulator migration). On first launch in the simulator, sample flights exist with v1-format String time fields.

    1. After launch, navigate to the Flights list (whichever tab shows it).
    2. Open any sample flight. Confirm the `blockTime` display value matches a sensible figure (e.g., "1:30" or "1.5h" — NOT "0:00").
    3. If `blockTime == 0` for every flight, migration policy did NOT run — STOP and report.
    4. Open the simulator's app sandbox via Finder (Xcode menu: Window > Devices and Simulators > Devices tab > select simulator > select Block-Time app > gear icon > Download Container) and verify the Core Data store file (`FlightDataModel.sqlite`) exists in the new location.

    **Step C — Verify orphan cleanup ran**

    1. In Xcode console, look for any log output related to orphan cleanup (if Task 2 added prints). If silent, verify by checking the App Group container — `blocktime.sqlite` (the Phase 1 SwiftData orphan) MUST NOT exist after first launch.
    2. Quit and relaunch the app — cleanup must NOT run again (UserDefaults flag prevents it). The app still launches normally.

    **Step D — Verify no SwiftData references compile-in**

    From the project root:
    ```
    grep -rln "import SwiftData" Block-Time/
    ```

    Must return ZERO results.

    **Step E — Build succeeds**

    ```
    xcodebuild -scheme Block-Time -destination 'generic/platform=iOS' build
    ```

    Exits 0.

    **What to do if migration runs incorrectly:**

    If `blockTime` is 0 for all flights after migration:
    - Custom policy class name in `.xcmappingmodel` may be wrong — check it is `FlightEntityMigrationPolicy` with NO module prefix
    - Or the `description.shouldInferMappingModelAutomatically` may still be true — verify in `FlightDatabaseService`
    - Or the `FlightEntityMigrationPolicy.swift` file may not be in the app target build phase — verify in Xcode's File Inspector

    If app crashes on launch with "Cannot find mapping model":
    - The `.xcmappingmodel` may not be in the app bundle — verify in Xcode: select the mapping model, File Inspector, confirm Target Membership includes Block-Time
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - "approved" — app launches, sample flights show non-zero blockTime, no SwiftData imports remain, no orphan blocktime.sqlite, build is green
    - "issue: {description}" — describe what failed and any console output
  </resume-signal>
  <acceptance_criteria>
    - `grep -rln "import SwiftData" Block-Time/ | wc -l` returns 0 (project-wide sweep)
    - `xcodebuild -scheme Block-Time -destination 'generic/platform=iOS' build` exits 0
    - User confirmation: app launches on simulator without crash
    - User confirmation: sample flights display non-zero blockTime (migration policy ran)
    - User confirmation: no Core Data error in console
    - User confirmation: orphan `blocktime.sqlite` no longer present in App Group container
  </acceptance_criteria>
  <done>App builds clean, launches on simulator, migration policy ran (non-zero time fields), orphan cleanup happened, zero SwiftData references.</done>
</task>

</tasks>

<verification>
- `xcodebuild -scheme Block-Time -destination 'generic/platform=iOS' build` exits 0
- `grep -rln "import SwiftData" Block-Time/` returns nothing
- All 8 SwiftData files (SchemaV1, FlightModel, AircraftModel, ModelContainerFactory, CoreDataMigrationService, CoreDataMigrationActor, LegacyFlightSnapshot, SwiftDataFlightRepository) are deleted
- `Block_TimeApp.swift` injects `CoreDataFlightRepository`, runs one-shot orphan cleanup
- `SplashScreenView.swift` no longer references any migration service
- `AppRepositoryEnvironment.swift` default stays `InMemoryFlightRepository` (verified read-only in Task 4 — likely no modification needed)
- App launches on simulator and existing-data flights display non-zero times (user verified)
</verification>

<success_criteria>
- REPO-07: All SwiftData infrastructure deleted — verified by file-not-exist checks + grep sweep
- REPO-08: App entry point injects `CoreDataFlightRepository` — verified by grep + simulator launch
</success_criteria>

<output>
After completion, create `.planning/phases/02-coredata-repository/02-04-SUMMARY.md` documenting:
- Final list of deleted files (with line counts at time of deletion for the record)
- The `Block_TimeApp.init()` orphan cleanup block as committed (exact code)
- The App Group identifier used (verify matches entitlements)
- Confirmation from checkpoint that migration ran correctly on simulator and what blockTime sample values were observed
- Whether Task 4 was a no-op verification or required a correction (and if so, what)
- Any deviations from D-22 through D-25
</output>
</content>
</invoke>