---
phase: quick
plan: 260524-hb5
type: execute
wave: 1
depends_on: []
files_modified:
  - Block-Time/Views/Components/FlightSectorRow.swift
autonomous: true
requirements: [QUICK-260524-hb5]

must_haves:
  truths:
    - "An INS|Simulator flight (isSpIns=true, isInstructingInAircraft=false) with spInsTime > 0 but simTime = 0 shows 'Sim' in the flight row label"
    - "A regular flight with simTime = 0 and spInsTime = 0 still shows 'Flt'"
    - "A simulator flight with simTime > 0 still shows 'Sim'"
  artifacts:
    - path: "Block-Time/Views/Components/FlightSectorRow.swift"
      provides: "Sim/Flt label that accounts for spInsTimeValue"
      contains: "sector.spInsTimeValue > 0"
  key_links:
    - from: "FlightSectorRow Sim/Flt label"
      to: "sector.spInsTimeValue"
      via: "OR condition in ternary"
      pattern: "spInsTimeValue > 0.*Sim"
---

<objective>
Fix the Sim/Flt label in FlightSectorRow so INS|Simulator flights are labelled "Sim".

Purpose: For an INS|Simulator flight (isSpIns=true, isInstructingInAircraft=false), spInsTime > 0 but simTime can be 0, so the row currently shows "Flt" incorrectly. It should show "Sim".
Output: Updated FlightSectorRow.swift line 218.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@Block-Time/Views/Components/FlightSectorRow.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Include spInsTimeValue in the Sim/Flt label condition</name>
  <files>Block-Time/Views/Components/FlightSectorRow.swift</files>
  <action>
At line 218, change:

    Text(sector.simTimeValue > 0 ? "Sim" : "Flt")

to:

    Text((sector.simTimeValue > 0 || sector.spInsTimeValue > 0) ? "Sim" : "Flt")

This is the only change. Do not alter any other logic, styling, or surrounding code. INS|Simulator flights have spInsTime > 0 but may have simTime = 0, so the existing condition wrongly shows "Flt"; the OR clause makes them show "Sim".
  </action>
  <verify>
    <automated>grep -n 'sector.simTimeValue > 0 || sector.spInsTimeValue > 0' "Block-Time/Views/Components/FlightSectorRow.swift"</automated>
  </verify>
  <done>Line 218 reads `Text((sector.simTimeValue > 0 || sector.spInsTimeValue > 0) ? "Sim" : "Flt")`; no other lines changed.</done>
</task>

</tasks>

<verification>
- grep confirms the new OR condition is present on the Sim/Flt label line.
- No other lines in FlightSectorRow.swift modified (single-line diff).
</verification>

<success_criteria>
- INS|Simulator flights (spInsTime > 0, simTime = 0) display "Sim".
- Regular flights (simTime = 0, spInsTime = 0) still display "Flt".
- Simulator flights (simTime > 0) still display "Sim".
- Change is one line in one file.
</success_criteria>

<output>
After completion, create `.planning/quick/260524-hb5-fix-flightsectorrow-to-show-sim-for-ins-/260524-hb5-SUMMARY.md`
</output>
