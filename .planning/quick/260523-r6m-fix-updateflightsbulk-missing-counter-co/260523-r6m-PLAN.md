---
phase: quick
plan: 260523-r6m
type: execute
wave: 1
depends_on: []
files_modified:
  - Block-Time/Services/FlightDatabaseService.swift
autonomous: false
requirements:
  - QUICK-r6m-01
must_haves:
  truths:
    - "Bulk-editing custom field values in BulkEditSheet persists to Core Data"
    - "Bulk update clears existing counter columns before writing new values (so blanks erase prior data)"
    - "Single-flight updateFlight behaviour is unchanged"
  artifacts:
    - path: "Block-Time/Services/FlightDatabaseService.swift"
      provides: "updateFlightsBulk now writes counter columns"
      contains: "flight.setCounter"
  key_links:
    - from: "BulkEditSheet Save"
      to: "FlightDatabaseService.updateFlightsBulk"
      via: "counter column write loop"
      pattern: "for \\(columnIndex, value\\) in updatedSector\\.counterEntries"
---

<objective>
Fix `FlightDatabaseService.updateFlightsBulk` so that bulk edits persist custom field (counter) values. Currently the bulk path writes every scalar field but omits the 10 counter columns, so values entered in BulkEditSheet are silently discarded on Save.

Purpose: Make BulkEditSheet's custom field editing actually save (parity with single-flight `updateFlight`).
Output: 4-line insertion in `updateFlightsBulk`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@Block-Time/Services/FlightDatabaseService.swift

<interfaces>
<!-- The reference implementation already exists in updateFlight (same file, lines 371-375): -->

```swift
// Clear all counter columns then write current values
for i in 1...10 { flight.setCounter(i, value: nil) }
for (columnIndex, value) in sector.counterEntries where !value.isEmpty {
    flight.setCounter(columnIndex, value: value)
}
```

In `updateFlightsBulk` the per-flight variable is `updatedSector` (not `sector`).
The insertion point is immediately after `flight.modifiedAt = Date()` at line 449,
inside the `for flight in flights` loop, before the closing brace at line 450.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add counter column clear-and-rewrite to updateFlightsBulk</name>
  <files>Block-Time/Services/FlightDatabaseService.swift</files>
  <action>
    In `Block-Time/Services/FlightDatabaseService.swift`, inside `updateFlightsBulk(_:)`, locate the `for flight in flights { ... }` loop (starts ~line 411). Immediately after the line `flight.modifiedAt = Date()` (~line 449) and before the loop's closing `}`, insert the following 4 lines (match indentation of surrounding `flight.` assignments):

    ```swift

    // Clear all counter columns then write current values
    for i in 1...10 { flight.setCounter(i, value: nil) }
    for (columnIndex, value) in updatedSector.counterEntries where !value.isEmpty {
        flight.setCounter(columnIndex, value: value)
    }
    ```

    Do NOT modify any other field assignments, the fetch request, the save call, the notification post, or the error handling. Do NOT touch `updateFlight` or any other function — the reference implementation there is already correct.

    Use the variable name `updatedSector` (NOT `sector`) — that is the local name used in `updateFlightsBulk`'s loop. Using `sector` will not compile.
  </action>
  <verify>
    <automated>grep -n "updatedSector.counterEntries" Block-Time/Services/FlightDatabaseService.swift</automated>
    Expected: one match inside `updateFlightsBulk`. Also confirm `grep -c "flight.setCounter" Block-Time/Services/FlightDatabaseService.swift` returns at least 3 (one in addFlight, one in updateFlight, one new in updateFlightsBulk pair = 4 actual setCounter call sites across the file).
  </verify>
  <done>
    The 4-line block exists inside the `for flight in flights` loop of `updateFlightsBulk`, immediately after `flight.modifiedAt = Date()`. References `updatedSector.counterEntries`. No other lines changed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Counter column writes added to `updateFlightsBulk` so BulkEditSheet custom field edits now persist.</what-built>
  <how-to-verify>
    1. Build and run on simulator or device.
    2. Open the Flights list and tap Edit, select 2+ flights, tap the bulk Edit button to open BulkEditSheet.
    3. In the custom fields section, enter a value for at least one custom field (e.g. set a counter to "5" or text to "test").
    4. Tap Save.
    5. Reopen any of the edited flights individually — the new custom field value should appear and be persisted.
    6. Repeat with a custom field cleared (set to empty) — confirm the previous value is erased after bulk Save.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what didn't work.</resume-signal>
</task>

</tasks>

<verification>
- Code change limited to a single insertion inside `updateFlightsBulk`.
- Bulk-edited custom field values persist to Core Data and survive reopen.
- Clearing a custom field via bulk edit removes the previous value (clear-then-rewrite pattern).
- Single-flight `updateFlight` path unchanged.
</verification>

<success_criteria>
- `grep -n "updatedSector.counterEntries" Block-Time/Services/FlightDatabaseService.swift` returns a match.
- Human verification (Task 2) passes: bulk edit of custom field values persists on reopen.
</success_criteria>

<output>
After completion, append entry to `.planning/STATE.md` Quick Tasks Completed table:
| 260523-r6m | Fix updateFlightsBulk missing counter column writes | 2026-05-23 | {commit} | [260523-r6m-fix-updateflightsbulk-missing-counter-co](./quick/260523-r6m-fix-updateflightsbulk-missing-counter-co/) |
</output>
