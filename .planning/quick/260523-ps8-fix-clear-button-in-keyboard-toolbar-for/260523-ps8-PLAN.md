---
phase: quick-260523-ps8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Block-Time/Views/Components/AddFlightView/FlightTimeFields.swift
autonomous: true
requirements: [QUICK-260523-PS8]
must_haves:
  truths:
    - "Tapping the keyboard toolbar Clear button while a ModernRemarksField is focused empties the field"
    - "Both the Remarks field and custom Text-type fields (which use ModernRemarksField) respond to Clear"
  artifacts:
    - path: "Block-Time/Views/Components/AddFlightView/FlightTimeFields.swift"
      provides: "ModernRemarksField with working Clear action wired to keyboardToolbar"
      contains: "clear: { value = \"\" }"
  key_links:
    - from: "ModernRemarksField.onChange(editorFocused)"
      to: "KeyboardToolbarState.fieldDidFocus(clear:)"
      via: "closure that sets value = \"\""
      pattern: "fieldDidFocus\\(clear:\\s*\\{\\s*value\\s*=\\s*\"\"\\s*\\}\\)"
---

<objective>
Fix the keyboard toolbar Clear button so it actually wipes the ModernRemarksField content when tapped.

Purpose: Currently the Clear action passed by ModernRemarksField is a no-op `{}`, so the user taps Clear and nothing happens. This affects the Remarks field and every custom Text-type field in AddFlightView (which reuse ModernRemarksField).

Output: Single-line change in `FlightTimeFields.swift` line 441 wiring Clear to `{ value = "" }`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Block-Time/Views/Components/AddFlightView/FlightTimeFields.swift

<interfaces>
<!-- ModernRemarksField has @Binding var value: String -->
<!-- KeyboardToolbarState.fieldDidFocus(clear: @escaping () -> Void) registers the active field's clear action -->
<!-- The toolbar's Clear button invokes whichever closure is currently registered -->

Current code (line 438-443 in FlightTimeFields.swift):
```swift
.onChange(of: editorFocused) { _, isFocused in
    if isFocused {
        // Remarks has no Clear button — pass a no-op clear action
        keyboardToolbar?.fieldDidFocus(clear: {})
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ModernRemarksField Clear action to empty the bound value</name>
  <files>Block-Time/Views/Components/AddFlightView/FlightTimeFields.swift</files>
  <action>
    In `ModernRemarksField`, locate the `.onChange(of: editorFocused)` block (around line 438-443).

    Replace:
    ```swift
    // Remarks has no Clear button — pass a no-op clear action
    keyboardToolbar?.fieldDidFocus(clear: {})
    ```

    With:
    ```swift
    keyboardToolbar?.fieldDidFocus(clear: { value = "" })
    ```

    Remove the stale comment ("Remarks has no Clear button — pass a no-op clear action") since the new behaviour does provide a Clear action.

    Do NOT change any other field, file, or behaviour. Do NOT touch the parent toolbar wiring — `KeyboardToolbarState.fieldDidFocus(clear:)` already handles invoking the registered closure when the toolbar's Clear button is tapped.

    Per project rule: invoke the `swiftui-pro` skill via the Skill tool before editing this Swift file.
  </action>
  <verify>
    <automated>grep -n 'fieldDidFocus(clear: { value = "" })' Block-Time/Views/Components/AddFlightView/FlightTimeFields.swift</automated>
  </verify>
  <done>
    - The line `keyboardToolbar?.fieldDidFocus(clear: { value = "" })` exists in ModernRemarksField.
    - The stale "no-op clear action" comment is gone.
    - No other code in the file was modified.
    - Nelson will build locally and confirm Clear now empties the Remarks field and custom Text-type fields in AddFlightView.
  </done>
</task>

</tasks>

<verification>
- Grep confirms the new closure body wires `value = ""`.
- Grep confirms no remaining `fieldDidFocus(clear: {})` no-op call in ModernRemarksField.
- File still compiles (Nelson builds locally — do not run `xcodebuild`).
</verification>

<success_criteria>
- Tapping Clear in the keyboard toolbar while editing a Remarks field clears its text.
- Tapping Clear while editing a custom Text-type field (which also uses ModernRemarksField) clears its text.
- No other field's Clear behaviour changes.
</success_criteria>

<output>
After completion, create `.planning/quick/260523-ps8-fix-clear-button-in-keyboard-toolbar-for/260523-ps8-SUMMARY.md`
</output>
