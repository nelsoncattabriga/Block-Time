---
id: 260524-do7
type: quick
autonomous: true
files_modified:
  - Block-Time/ViewModels/FlightTimeExtractorViewModel.swift
  - Block-Time/Views/Components/FlightSectorRow.swift
---

<objective>
Fix two INS|Simulator bugs: (1) empty simInsTime field causes full INS time to be saved as simTime, (2) INS|Sim flights with no blockTime/simTime are incorrectly dimmed as future/rostered flights.
</objective>

<context>
@Block-Time/ViewModels/FlightTimeExtractorViewModel.swift
@Block-Time/Views/Components/FlightSectorRow.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix simInsTime fallback and INS flight dimming</name>
  <files>
    Block-Time/ViewModels/FlightTimeExtractorViewModel.swift
    Block-Time/Views/Components/FlightSectorRow.swift
  </files>
  <action>
**Fix 1 — FlightTimeExtractorViewModel.swift**

Search for all three occurrences of:
```swift
let sim = min(Double(simInsTime) ?? ins, ins)
```
Replace each with:
```swift
let sim = min(Double(simInsTime) ?? 0, ins)
```
There are exactly 3 occurrences (approximately lines 1584, 1897, 2815). Change all three — do not miss any.

**Fix 2 — FlightSectorRow.swift**

In `calculateIsFutureFlight()`, find the guard:
```swift
guard blockTime == 0 && simTime == 0 else {
    return false
}
```
Replace with:
```swift
guard blockTime == 0 && simTime == 0 && sector.spInsTimeValue == 0 else {
    return false
}
```
`sector` here is the `FlightSector` model instance passed into the row. Use `spInsTimeValue` (the existing computed Double property) — do not use `isSpInsOnly` as it has a different threshold condition.
  </action>
  <verify>
    Grep confirms no remaining `?? ins` in the sim calculation lines:
    `grep -n "?? ins" Block-Time/ViewModels/FlightTimeExtractorViewModel.swift` → no matches.
    Grep confirms the guard includes spInsTimeValue:
    `grep -n "spInsTimeValue" Block-Time/Views/Components/FlightSectorRow.swift` → shows the guard line.
  </verify>
  <done>
    - All 3 occurrences of `?? ins` changed to `?? 0` in FlightTimeExtractorViewModel.swift
    - calculateIsFutureFlight guard includes `&& sector.spInsTimeValue == 0`
    - An INS|Sim flight with empty SIM field saves 0 to simTime (not the full INS value)
    - An INS|Sim flight with spInsTime > 0, blockTime == 0, simTime == 0 is NOT dimmed
  </done>
</task>

</tasks>

<success_criteria>
- Saving an INS|Simulator flight with blank SIM time field writes 0:00 to simTime, not a copy of INS time
- INS|Simulator flights appear at full opacity in FlightsView and FlightsSplitView
- No regression to non-INS flights (regular future/rostered flights still dim correctly)
</success_criteria>

<output>
After completion, create `.planning/quick/260524-do7-fix-ins-simulator-save-and-dimming-bugs/260524-do7-SUMMARY.md`
</output>
