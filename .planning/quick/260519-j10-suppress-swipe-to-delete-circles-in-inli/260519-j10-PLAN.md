---
phase: quick-260519-j10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Block-Time/Views/Screens/Settings/SettingsView.swift
autonomous: true
requirements:
  - QUICK-260519-J10
must_haves:
  truths:
    - "Custom Fields list in InlineCustomFieldsView shows drag handles only (no red delete circles)"
    - "Rows can still be reordered via drag handles"
    - "Delete continues to work from inside FieldEditSheet (existing onDelete callback)"
    - "No other code (CustomFieldsSettingsView etc.) is changed"
  artifacts:
    - path: "Block-Time/Views/Screens/Settings/SettingsView.swift"
      provides: "InlineCustomFieldsView with .onDelete removed from ForEach and .deleteDisabled(true) added to row Button"
      contains: ".deleteDisabled(true)"
  key_links:
    - from: "InlineCustomFieldsView List ForEach row Button"
      to: ".deleteDisabled(true)"
      via: "SwiftUI row modifier"
      pattern: "\\.deleteDisabled\\(true\\)"
---

<objective>
Suppress the red swipe-to-delete circles that appear in `InlineCustomFieldsView` when `.editMode` is active, while keeping the drag handles for reordering. Delete remains available via the existing `FieldEditSheet` `onDelete:` callback.

Purpose: The current `.editMode = .active` displays BOTH drag handles AND red delete circles on every row, which clutters the UI. Removing `.onDelete` from the `ForEach` and adding `.deleteDisabled(true)` on the row removes the circles while preserving drag-reorder.

Output: Updated `InlineCustomFieldsView` in `Block-Time/Views/Screens/Settings/SettingsView.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@Block-Time/Views/Screens/Settings/SettingsView.swift

# Per project CLAUDE.md: invoke the `swiftui-pro` skill before writing or editing any Swift/SwiftUI code.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suppress swipe-to-delete circles in InlineCustomFieldsView</name>
  <files>Block-Time/Views/Screens/Settings/SettingsView.swift</files>
  <action>
Before editing, invoke the `swiftui-pro` skill via the Skill tool (per project CLAUDE.md requirement).

Edit `InlineCustomFieldsView` (around lines 2441–2530) ONLY. Do NOT touch `CustomFieldsSettingsView` or any other code.

Two precise changes inside the `List { ForEach(service.definitions) { definition in ... } }` block:

1. **Add `.deleteDisabled(true)` to each row.** Append it to the row `Button`'s modifier chain — directly after `.listRowBackground(Color(.secondarySystemBackground))`. Result:

   ```swift
   .buttonStyle(.plain)
   .listRowBackground(Color(.secondarySystemBackground))
   .deleteDisabled(true)
   ```

2. **Remove the entire `.onDelete { ... }` modifier** from the `ForEach` (currently lines ~2477–2482):

   ```swift
   .onDelete { offsets in
       let columnIndices = offsets.map { service.definitions[$0].columnIndex }
       for columnIndex in columnIndices {
           service.remove(columnIndex: columnIndex)
       }
   }
   ```

   Delete this whole block. Keep `.onMove { ... }` exactly as-is.

Leave everything else untouched:
- `.environment(\.editMode, .constant(.active))` stays (it powers the drag handles)
- `.onMove { ... }` stays (drag-reorder behavior)
- `FieldEditSheet`'s `onDelete:` callback (lines ~2509–2511) stays — that is how the user deletes a field now.

Do NOT add any new imports, helpers, or refactors. This is a two-line surgical change.
  </action>
  <verify>
    <automated>grep -n "deleteDisabled(true)" Block-Time/Views/Screens/Settings/SettingsView.swift && ! grep -A1 "ForEach(service.definitions)" Block-Time/Views/Screens/Settings/SettingsView.swift | grep -q "onDelete" && grep -c "\.onMove" Block-Time/Views/Screens/Settings/SettingsView.swift</automated>
  </verify>
  <done>
- `.deleteDisabled(true)` is present on the row `Button` inside `InlineCustomFieldsView`'s `ForEach`.
- `.onDelete { ... }` block is removed from `InlineCustomFieldsView`'s `ForEach`.
- `.onMove { ... }` and `.environment(\.editMode, .constant(.active))` are unchanged.
- `CustomFieldsSettingsView` is unchanged.
- File still compiles (Nelson will build locally).
  </done>
</task>

</tasks>

<verification>
- Drag handles still visible on each row in `InlineCustomFieldsView` (Settings → Crew & Ops → Custom Fields).
- Red delete circles no longer appear.
- Reorder via drag still works.
- Tapping a row still opens `FieldEditSheet`, which still offers Delete.
- No changes to `CustomFieldsSettingsView` or any other view.
</verification>

<success_criteria>
- Single-file diff limited to `InlineCustomFieldsView` in `Block-Time/Views/Screens/Settings/SettingsView.swift`.
- Two changes only: `.onDelete` block removed; `.deleteDisabled(true)` added to row Button.
- No regressions to drag-reorder, edit-via-tap, or delete-via-sheet flows.
</success_criteria>

<output>
After completion, create `.planning/quick/260519-j10-suppress-swipe-to-delete-circles-in-inli/260519-j10-SUMMARY.md`.
</output>
