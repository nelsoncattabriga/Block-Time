---
phase: quick-260519-j10
plan: 01
subsystem: Settings UI
tags: [swiftui, custom-fields, list, edit-mode]
key-files:
  modified:
    - Block-Time/Views/Screens/Settings/SettingsView.swift
decisions:
  - Delete via FieldEditSheet only; swipe-delete removed from inline list
metrics:
  duration: ~5 min
  completed: 2026-05-19
  tasks_completed: 1
  tasks_total: 1
  files_changed: 1
---

# Quick Task 260519-j10: Suppress Swipe-to-Delete Circles in InlineCustomFieldsView

**One-liner:** Removed `.onDelete` from `InlineCustomFieldsView` `ForEach` and added `.deleteDisabled(true)` to each row, eliminating red delete circles while keeping drag-reorder via `.editMode`.

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| 1 | Suppress swipe-to-delete circles in InlineCustomFieldsView | 960bc66 |

## Changes Made

**File:** `Block-Time/Views/Screens/Settings/SettingsView.swift`

Two surgical edits inside `InlineCustomFieldsView` only:

1. Added `.deleteDisabled(true)` to each row `Button` after `.listRowBackground(Color(.secondarySystemBackground))`.
2. Removed the entire `.onDelete { offsets in ... }` block from the `ForEach`.

Unchanged:
- `.environment(\.editMode, .constant(.active))` — drag handles still appear
- `.onMove { source, destination in ... }` — drag-reorder still works
- `FieldEditSheet`'s `onDelete:` callback — delete still available via tap-to-edit sheet
- `CustomFieldsSettingsView` — untouched

## Deviations from Plan

**Prerequisite merge:** This worktree was behind `main` by two commits (`36c8453`, `7be7a1c` from quick task ir7 which converted `InlineCustomFieldsView` from `VStack` to `List`). Merged `main` into the worktree branch before applying j10 changes. No conflicts.

## Known Stubs

None.

## Self-Check: PASSED

- `960bc66` exists in git log.
- `.deleteDisabled(true)` present at line 2473 of `SettingsView.swift`.
- No `.onDelete` block in `InlineCustomFieldsView`'s `ForEach`.
- `.onMove` and `.editMode` unchanged.
- `CustomFieldsSettingsView` unchanged.
