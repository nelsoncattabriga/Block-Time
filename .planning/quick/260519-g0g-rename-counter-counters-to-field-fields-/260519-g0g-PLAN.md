---
phase: quick
plan: 260519-g0g
type: execute
wave: 1
depends_on: []
files_modified:
  - Block-Time/Views/Screens/Settings/SettingsView.swift
  - Block-Time/Views/Components/AddFlightView/CrewOpsCard.swift
  - Block-Time/Views/Components/Dashboard/CustomCounterDashboardCard.swift
  - Block-Time/Models/DashboardCardID.swift
  - Block-Time/ViewModels/FlightTimeExtractorViewModel.swift
autonomous: true
requirements: []

must_haves:
  truths:
    - "Settings toggle label reads 'Use Custom Fields'"
    - "Add/Edit/Delete sheet titles say 'Add Field', 'Edit Field', 'Delete Field'"
    - "Navigation title reads 'Custom Fields'"
    - "Section header reads 'Field Details'"
    - "Empty-state messages say 'field' not 'counter'"
    - "Confirmation dialogs say 'field' not 'counter'"
    - "UserDefaults keys, stored JSON, CounterType, CustomCounterService, CustomCounterDefinition, DashboardCardID.customCounter are unchanged"
  artifacts:
    - path: "Block-Time/Views/Screens/Settings/SettingsView.swift"
      provides: "Renamed user-facing strings and Swift identifiers"
    - path: "Block-Time/Views/Components/AddFlightView/CrewOpsCard.swift"
      provides: "Renamed view types and function names"
    - path: "Block-Time/Views/Components/Dashboard/CustomCounterDashboardCard.swift"
      provides: "Renamed fallback string"
    - path: "Block-Time/Models/DashboardCardID.swift"
      provides: "Renamed fallback string"
    - path: "Block-Time/ViewModels/FlightTimeExtractorViewModel.swift"
      provides: "Renamed fallback string"
  key_links:
    - from: "SettingsView.swift"
      to: "CounterEditSheet (now FieldEditSheet)"
      via: ".sheet presentation"
      pattern: "FieldEditSheet"
    - from: "CrewOpsCard.swift counterRow"
      to: "CounterTimeField/CounterDecimalField/CounterIntegerField (now FieldTimeField etc.)"
      via: "switch dispatch"
      pattern: "fieldRow"
---

<objective>
Rename all user-visible "Counter/Counters" labels to "Field/Fields" throughout the codebase, and rename the corresponding Swift identifiers (struct names, enum names, function names) that are safe to change. Storage keys, Core Data attributes, CounterType, CustomCounterService, CustomCounterDefinition, and DashboardCardID.customCounter are NOT touched.

Purpose: Align the UI language with "Custom Fields" terminology without breaking stored data.
Output: Updated Swift files with consistent "Field" naming in UI and code identifiers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename strings and identifiers in SettingsView.swift</name>
  <files>Block-Time/Views/Screens/Settings/SettingsView.swift</files>
  <action>
Make these exact replacements. Do NOT change anything else in the file.

User-facing string literals:
- `"Use Custom Counters"` → `"Use Custom Fields"`
- `"No counters added yet."` → `"No fields added yet."`
- `"Add Counter"` (all occurrences) → `"Add Field"`
- `"No counters defined. Tap \"Add Counter\" to create one."` → `"No fields defined. Tap \"Add Field\" to create one."`
- `"Counters appear in the Add/Edit flight form and as selectable Dashboard cards."` → `"Fields appear in the Add/Edit flight form and as selectable Dashboard cards."`
- `.navigationTitle("Custom Counters")` → `.navigationTitle("Custom Fields")`
- `"Counter Details"` (Section header) → `"Field Details"`
- `"Add Counter"` (in title var, .add case) → `"Add Field"`
- `"Edit Counter"` (in title var, .edit case) → `"Edit Field"`
- `"Delete Counter"` (button text) → `"Delete Field"`
- `"Delete this counter?"` (confirmationDialog title) → `"Delete this field?"`
- `"This will remove the counter and all its data cannot be recovered."` → `"This will remove the field and all its data cannot be recovered."`

Swift identifiers (safe to rename — not stored anywhere):
- struct `InlineCustomCountersView` → `InlineCustomFieldsView`
  - Also update the call site at line ~696: `InlineCustomCountersView()` → `InlineCustomFieldsView()`
- struct `CustomCountersSettingsView` → `CustomFieldsSettingsView`
- private enum `CounterEditMode` → `FieldEditMode`
  - Update all references to `CounterEditMode` throughout this file
- private struct `CounterEditSheet` → `FieldEditSheet`
  - Update all references to `CounterEditSheet` throughout this file
  - `let mode: CounterEditMode` → `let mode: FieldEditMode`
  - `init(mode: CounterEditMode, ...)` → `init(mode: FieldEditMode, ...)`

MARK comments:
- `// MARK: - Counter Edit Sheet` → `// MARK: - Field Edit Sheet`
- `// MARK: - Custom Counters Settings View` → `// MARK: - Custom Fields Settings View`
- `// MARK: - Inline Custom Counters (embedded in Crew & Ops card)` → `// MARK: - Inline Custom Fields (embedded in Crew & Ops card)`
- `// MARK: - Full-page Custom Counters (iPad split view, kept for future use)` → `// MARK: - Full-page Custom Fields (iPad split view, kept for future use)`

DO NOT rename: CounterType, CustomCounterService, CustomCounterDefinition, storageKey, UserDefaults keys, columnIndex references, counterType (local variable name in ForEach), any @AppStorage keys.
  </action>
  <verify>grep -n "CounterEditMode\|CounterEditSheet\|InlineCustomCountersView\|CustomCountersSettingsView\|\"Use Custom Counters\"\|\"Add Counter\"\|\"Edit Counter\"\|\"Delete Counter\"\|\"Delete this counter\"\|\"Counter Details\"\|\"Custom Counters\"\|\"No counters" "Block-Time/Views/Screens/Settings/SettingsView.swift" | grep -v "// OLD\|CounterType\|counterType"</verify>
  <done>grep returns no matches (all target strings replaced). Build compiles with no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rename view types and function in CrewOpsCard.swift</name>
  <files>Block-Time/Views/Components/AddFlightView/CrewOpsCard.swift</files>
  <action>
Make these exact replacements. Do NOT change anything else in the file.

Swift identifiers:
- struct `CounterTimeField` → `FieldTimeField`
  - Update all call sites: `CounterTimeField(` → `FieldTimeField(`
- struct `CounterDecimalField` → `FieldDecimalField`
  - Update all call sites: `CounterDecimalField(` → `FieldDecimalField(`
- struct `CounterIntegerField` → `FieldIntegerField`
  - Update all call sites: `CounterIntegerField(` → `FieldIntegerField(`
- function `counterRow(for:viewModel:keyboardToolbar:)` → `fieldRow(for:viewModel:keyboardToolbar:)`
  - Update the call site inside the ForEach in CrewOpsCard body: `counterRow(` → `fieldRow(`

MARK comments:
- `// MARK: - Counter field types (shared by CrewOpsCard)` → `// MARK: - Field types (shared by CrewOpsCard)`
- `// Custom counters` comment (inline, in the body) — leave as-is (it describes the feature, not the type)
- `// MARK: - Counter row dispatcher` → `// MARK: - Field row dispatcher`

DO NOT rename: CounterType enum references (still used as parameter types), CustomCounterService, CustomCounterDefinition, counterValues (ViewModel property), counterEntries.
  </action>
  <verify>grep -n "CounterTimeField\|CounterDecimalField\|CounterIntegerField\|counterRow" "Block-Time/Views/Components/AddFlightView/CrewOpsCard.swift"</verify>
  <done>grep returns no matches. Build compiles with no new errors.</done>
</task>

<task type="auto">
  <name>Task 3: Rename fallback strings in DashboardCardID, CustomCounterDashboardCard, and FlightTimeExtractorViewModel</name>
  <files>
    Block-Time/Models/DashboardCardID.swift
    Block-Time/Views/Components/Dashboard/CustomCounterDashboardCard.swift
    Block-Time/ViewModels/FlightTimeExtractorViewModel.swift
  </files>
  <action>
Make these exact replacements. Only touch the listed strings — nothing else.

In DashboardCardID.swift:
- Line ~88: `?? "Counter"` (in displayName computed var, customCounterColumnIndex branch) → `?? "Field"`

In CustomCounterDashboardCard.swift:
- Line ~45: `definition?.label ?? "Counter"` → `definition?.label ?? "Field"`
- Line ~87: `Text("Counter unavailable")` → `Text("Field unavailable")`

In FlightTimeExtractorViewModel.swift:
- Line ~2196: `?? "Counter"` (the label fallback when definition is nil) → `?? "Field"`

DO NOT rename any other identifiers, function names, or stored keys in these files.
  </action>
  <verify>grep -n '"Counter"' "Block-Time/Models/DashboardCardID.swift" "Block-Time/Views/Components/Dashboard/CustomCounterDashboardCard.swift" "Block-Time/ViewModels/FlightTimeExtractorViewModel.swift"</verify>
  <done>grep returns no matches. Build compiles with no new errors.</done>
</task>

</tasks>

<verification>
After all tasks:
1. grep -rn --include="*.swift" '"Counter"\|"Counters"\|"counter"\|"counters"' Block-Time/ | grep -v "// OLD\|arrow\.counter\|CustomCounterService\|CustomCounterDefinition\|customCounter\|CounterType\|legacyCounter\|counterEntries\|counterValues\|counterColumn\|counterCount\|customCounterDef\|counterDefinitions\|counterMigr\|builtCounter\|addTotalCounter\|counterIndex\|counter1\|counter2\|counter3\|counter4\|counter5\|counter6\|counter7\|counter8\|counter9\|counter10\|importMappingView"
   → Should return zero matches of user-visible counter/counters strings

2. No references to CounterEditMode, CounterEditSheet, InlineCustomCountersView, CustomCountersSettingsView, CounterTimeField, CounterDecimalField, CounterIntegerField, counterRow exist outside of comments.
</verification>

<success_criteria>
- All user-visible "Counter/Counters" strings replaced with "Field/Fields"
- Toggle label in Settings reads "Use Custom Fields"
- Sheet titles: "Add Field", "Edit Field", "Delete Field"
- Nav title: "Custom Fields"
- Section header: "Field Details"
- Empty state: "No fields added yet." / "No fields defined..."
- Footer: "Fields appear in the Add/Edit flight form..."
- Confirmation: "Delete this field?" and "This will remove the field..."
- Swift type names updated: FieldEditSheet, FieldEditMode, InlineCustomFieldsView, CustomFieldsSettingsView, FieldTimeField, FieldDecimalField, FieldIntegerField, fieldRow
- CounterType, CustomCounterService, CustomCounterDefinition, DashboardCardID.customCounter, all UserDefaults keys, all stored JSON keys: UNCHANGED
- Project compiles without new errors
</success_criteria>

<output>
After completion, create `.planning/quick/260519-g0g-rename-counter-counters-to-field-fields-/260519-g0g-SUMMARY.md`
</output>
