---
phase: quick
plan: 260522-ke8
type: execute
wave: 1
depends_on: []
files_modified:
  - Block-Time/Views/Screens/Settings/SettingsView.swift
  - Block-Time/Views/Components/AddFlightView/CrewOpsCard.swift
  - Block-Time/Views/Screens/Settings/ImportMappingView.swift
  - Block-Time/Services/FileImportService.swift
  - Block-Time/Views/Screens/FrozenColumnSpreadsheetView.swift
  - Block-Time/Views/Screens/LogbookSpreadsheetView.swift
  - Block-Time/Views/Components/Dashboard/DashboardCardView.swift
  - Block-Time/Views/Components/Dashboard/DashboardEditSheet.swift
  - Block-Time/ViewModels/FlightTimeExtractorViewModel.swift
  - Block-Time/Models/FlightLogbook.swift
  - Block-Time/Services/FlightDatabaseService.swift
  - Block-Time/Services/MigrationImportService.swift
  - Block-Time/Views/Screens/FlightSectorEditScreen.swift
  - Block-Time/Views/Components/Dashboard/CustomCountCard.swift
autonomous: true
requirements:
  - QUICK-260522-ke8
must_haves:
  truths:
    - "Settings no longer shows the legacy 'Use Custom Fields' toggle row gated on logCustomCount or the customCountLabel text field — only the Custom Fields section remains"
    - "Add/Edit Flight no longer renders the legacy single customCount FieldIntegerField; only InlineCustomFieldsView-driven counters appear"
    - "ImportMappingView's logbookFields no longer offers 'Custom Count' as a selectable destination"
    - "Importing a legacy Block-Time CSV with a 'Custom Count' column auto-maps that header to Counter1 (not to a removed Custom Count field)"
    - "Regular CSV export (non-backup path) has no standalone 'Custom Count' column — only Counter1…CounterN columns from definitions"
    - "FrozenColumnSpreadsheetView and LogbookSpreadsheetView show counter columns whenever CustomCounterService.shared.definitions is non-empty, regardless of the logCustomCount UserDefault"
    - "DashboardEditSheet and DashboardCardView gate the legacy .customCount card on !CustomCounterService.shared.definitions.isEmpty instead of logCustomCount"
    - "FlightSector struct compiles without a customCount stored property; all writers/readers go through counterEntries"
    - "FlightEntity.customCount Core Data attribute is unchanged; migrateLegacyCustomCounterToColumn1 still reads entity.customCount"
    - "UserDefaultsService still defines the logCustomCount key, default value, and read/write accessors (untouched)"
  artifacts:
    - path: "Block-Time/Models/FlightLogbook.swift"
      provides: "FlightSector without customCount stored property"
      contains: "struct FlightSector"
    - path: "Block-Time/Services/FileImportService.swift"
      provides: "Export without legacy Custom Count column; auto-detect maps 'custom count' header → Counter1"
    - path: "Block-Time/Services/FlightDatabaseService.swift"
      provides: "Write paths no longer assign flight.customCount from sector; migration code path untouched"
  key_links:
    - from: "ImportMappingView auto-detect"
      to: "Counter1 field"
      via: "headerLower == 'custom count' branch in createInitialMappings/header loop"
      pattern: "custom count.*Counter1"
    - from: "FlightSector init call sites"
      to: "counterEntries dictionary"
      via: "MigrationImportService folds v1 customCount into counterEntries[1]"
      pattern: "counterEntries\\[1\\]"
    - from: "Spreadsheet/Dashboard counter gates"
      to: "CustomCounterService.shared.definitions"
      via: "!definitions.isEmpty checks replace UserDefaults logCustomCount reads"
      pattern: "CustomCounterService.shared.definitions"
---

<objective>
Retire the legacy single `customCount` field from the user-facing UI, import/export, view models, and the FlightSector domain struct. The multi-counter system (Counter1–Counter10 via CustomCounterService) is the only path forward. The legacy `FlightEntity.customCount` Core Data attribute, the `logCustomCount` UserDefaults key, and the one-time migration code that copies customCount→counter1 MUST remain in place — they exist for migration safety.

Purpose: Eliminate dead/duplicate code paths now that Custom Fields are fully wired. Existing internal-only users (V1.20 has no custom fields) experience no data loss because Core Data still carries customCount and migration runs on first launch of the new build.

Output: A build where customCount is invisible to users and absent from FlightSector, while migration paths still function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Per CLAUDE.md: Do NOT run xcodebuild — Nelson builds locally. Use `swiftui-pro` skill before writing or editing any Swift code. Replace any `NavigationView` encountered (none expected here) with `NavigationStack`.
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Critical constraints (do NOT violate)
- FlightEntity.customCount Core Data attribute MUST remain.
- FlightDatabaseService.migrateLegacyCustomCounterToColumn1 (around line 1737) and the SplashScreenView legacy migration code (lines 91–180) MUST remain unchanged.
- UserDefaultsService logCustomCount and customCountLabel keys, defaults, getters, setters, and DTO fields MUST remain — migration and Splash code may still read them.
- CloudKitSettingsSyncService cloud_logCustomCount / cloud_customCountLabel keys MUST remain.
- MigrationImportService v1 settings DTO (logCustomCount?, customCountLabel?) and the customCount? per-flight field MUST remain — v1 backups can still arrive.
- DashboardCardID.customCount enum case MUST remain — legacy persisted dashboard configurations may still reference it; DashboardConfiguration.swift filters it out post-migration.

# Files already read by planner (exact lines/structures known)
- SettingsView.swift lines 680–710: ModernToggleRow "Use Custom Fields" + `if viewModel.logCustomCount { InlineCustomFieldsView() }`
- CrewOpsCard.swift lines 140–175: `if viewModel.logCustomCount && !viewModel.isPositioning` block contains a Divider, legacy FieldIntegerField (gated on `!migrated`), and the ForEach over CustomCounterService.shared.definitions. The ForEach must remain (gated instead on `!definitions.isEmpty`); the legacy FieldIntegerField + `customCountLabel` reference must be removed.
- ImportMappingView.swift line 593: `("Custom Count", "Custom numeric counter (e.g. PAX carried)", false, false)` — remove from the logbookFields array.
- ImportMappingView.swift line 1240: `} else if headerLower == "custom count" {` branch — change destination from `"Custom Count"` to `"Counter1"` (logbookFieldDescription "Counter 1") so legacy CSV exports auto-map to the new slot.
- FileImportService.swift lines 617–618: `customCountRaw` / `customCount` parsing local — remove (this is now handled by the auto-mapped Counter1 path via getValue("Counter1") in the existing 1...10 loop at lines 765–769).
- FileImportService.swift line 763: `customCount: isPositioning ? 0 : max(0, customCount)` — remove this argument from the FlightSector init call (initializer will lose the parameter in Task 6).
- FileImportService.swift line 1274: header row constant — remove the trailing `,Custom Count` segment.
- FileImportService.swift line 1318: `flight.customCount > 0 ? String(flight.customCount) : ""` row entry — remove.
- FrozenColumnSpreadsheetView.swift lines 276, 770: replace `UserDefaults.standard.bool(forKey: "logCustomCount") ? CustomCounterService.shared.definitions : []` with `CustomCounterService.shared.definitions` (always show columns when definitions exist).
- LogbookSpreadsheetView.swift lines 27, 109: same swap — counter count = definitions.count regardless of logCustomCount.
- DashboardEditSheet.swift lines 18, 26: replace `@AppStorage("logCustomCount")` + `($0 != .customCount || logCustomCount)` with `!CustomCounterService.shared.definitions.isEmpty` gate. Keep the .customCount filter behaviour: card visible only when definitions exist.
- DashboardCardView.swift lines 21, 102–103: replace `@AppStorage("logCustomCount")` + `if logCustomCount { CustomCountCard() }` with `if !CustomCounterService.shared.definitions.isEmpty { CustomCountCard() }`.
- FlightTimeExtractorViewModel.swift line 42 (`var customCount: Int = 0` inside FlightDraft), 161 (`@Published var customCount = 0`), 1632/1782/1947/2047/2179/2180/2597/2886/2942/3390/3456 — every read/write of `customCount` and the changes-summary entry must go. `customCountLabel` (lines 188, 463, 541–542, 858, 2180) is NOT removed — leave as-is because Splash migration still reads UserDefaults customCountLabel and the property is harmless without UI.
- FlightLogbook.swift lines 104, 115, 137, 173, 225, 335: remove `customCount` stored property, CodingKey, init parameter, init assignment, decoder line, and the `customCount: Int(entity.customCount)` argument inside `init(from entity:)`.
- FlightDatabaseService.swift lines 298, 370, 451, 1070, 1363: remove `flight.customCount = Int16(sector.customCount)` assignments and the `customCount: sector.customCount` init arg at line 1070. Line 2203: remove `customCount: Int(entity.customCount)` from the FlightSector init in the entity→sector converter. Lines 1737–1759 (migrateLegacyCustomCounterToColumn1) MUST remain untouched — they read `entity.customCount` directly via NSFetchRequest, which is independent of the FlightSector struct.
- MigrationImportService.swift line 479: replace `customCount: migrationFlight.customCount ?? 0` with `counterEntries: migrationFlight.customCount.map { $0 > 0 ? [1: String($0)] : [:] } ?? [:]` so v1 backups still land the value in Counter1. Keep `MigrationFlight.customCount: Int?` field on the DTO (line 63) unchanged so JSON decoding from v1 backups still works.
- FlightSectorEditScreen.swift line 379: remove `customCount: sector.customCount,` argument from the FlightSector init call.
- CustomCountCard.swift lines 96–97: this card sums `flight.customCount` for the legacy single-counter card. The card is still rendered (gated by definitions in Dashboard) but the data source must change. Replace with `Int(flight.counterEntries[1] ?? "") ?? 0` so the legacy card reflects Counter1 values (which is where migrated and new data live).

# Build cadence
Do NOT run xcodebuild. Edit, save, return — Nelson builds locally and reports any compiler errors.
</context>

<interfaces>
<!-- Key types the executor will touch. Extracted from codebase. -->

FlightSector init signature (Block-Time/Models/FlightLogbook.swift lines 137–143) — AFTER this plan, the `customCount: Int = 0,` parameter is gone; `counterEntries: [Int: String] = [:]` remains:
```swift
init(id: UUID? = nil, date: String, flightNumber: String, aircraftReg: String, aircraftType: String,
     fromAirport: String, toAirport: String, captainName: String, foName: String,
     so1Name: String? = nil, so2Name: String? = nil, blockTime: String,
     nightTime: String, p1Time: String, p1usTime: String, p2Time: String = "0.0", instrumentTime: String,
     simTime: String, spInsTime: String = "", isPilotFlying: Bool, isPositioning: Bool = false, isAIII: Bool = false, isRNP: Bool = false,
     isILS: Bool = false, isGLS: Bool = false, isNPA: Bool = false, remarks: String = "",
     dayTakeoffs: Int = 0, dayLandings: Int = 0, nightTakeoffs: Int = 0, nightLandings: Int = 0,
     outTime: String = "", inTime: String = "", scheduledDeparture: String = "", scheduledArrival: String = "",
     counterEntries: [Int: String] = [:], createdAt: Date? = nil)
```

CustomCounterService.shared.definitions: `[CustomCounterDefinition]` — the canonical source for whether multi-counters are active. Replace `UserDefaults.standard.bool(forKey: "logCustomCount")` gates with `!CustomCounterService.shared.definitions.isEmpty`.

FlightEntity.customCount: Int16 — Core Data attribute, NOT removed. Only the migration code (FlightDatabaseService.migrateLegacyCustomCounterToColumn1) and the raw database viewer (RawDatabaseView line 178) may still read it.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip legacy customCount UI from Settings and Add/Edit Flight</name>
  <files>Block-Time/Views/Screens/Settings/SettingsView.swift, Block-Time/Views/Components/AddFlightView/CrewOpsCard.swift</files>
  <action>
  Invoke the `swiftui-pro` skill before editing.

  SettingsView.swift (lines 680–710): Remove the "Use Custom Fields" ModernToggleRow (the entire block that binds to `viewModel.logCustomCount` and calls `viewModel.updateLogCustomCount`) and the surrounding `if viewModel.logCustomCount { InlineCustomFieldsView() }` wrapper. Replace with a direct render of `InlineCustomFieldsView()` (always shown). Do NOT remove `viewModel.updateLogCustomCount` from the ViewModel or any other Settings rows. Keep the surrounding card styling (.padding, .background, .cornerRadius, .shadow, .overlay) so the section visually matches the rest.

  CrewOpsCard.swift (lines 145–170): Replace the gate `if viewModel.logCustomCount && !viewModel.isPositioning {` with `if !CustomCounterService.shared.definitions.isEmpty && !viewModel.isPositioning {`. Delete the legacy single counter block (the `let migrated = ...` line and the `if !migrated { FieldIntegerField(label: viewModel.customCountLabel, value: Binding(...)) { ... } }` block). Keep the Divider and the `ForEach(CustomCounterService.shared.definitions) { definition in fieldRow(...) }` exactly as-is.

  Verify InlineCustomFieldsView is already imported / accessible from CrewOpsCard scope by reading the existing usage in `fieldRow(for:viewModel:keyboardToolbar:)` — no new imports expected.

  Do NOT touch UserDefaultsService, the logCustomCount key, or the customCountLabel key.
  </action>
  <verify>
    <automated>MISSING — Nelson builds locally per CLAUDE.md. Manual verification: open Settings → no "Use Custom Fields" toggle, Custom Fields section visible directly. Open Add Flight → counter rows appear when definitions exist, no legacy single PAX field.</automated>
  </verify>
  <done>SettingsView.swift contains no reference to `viewModel.logCustomCount` or `viewModel.customCountLabel` within the modified card; CrewOpsCard.swift contains no reference to `viewModel.customCount`, `viewModel.customCountLabel`, or the `legacyCounterMigratedToColumn1` UserDefault.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ImportMappingView and FileImportService import/export paths</name>
  <files>Block-Time/Views/Screens/Settings/ImportMappingView.swift, Block-Time/Services/FileImportService.swift</files>
  <action>
  Invoke the `swiftui-pro` skill before editing.

  ImportMappingView.swift line 593: Delete the `("Custom Count", "Custom numeric counter (e.g. PAX carried)", false, false)` entry from the `logbookFields` array in `createInitialMappings`.

  ImportMappingView.swift line 1240 (header auto-detect loop): Change the `headerLower == "custom count"` branch so it maps to Counter1:
  ```swift
  } else if headerLower == "custom count" {
      mappings.append(FieldMapping(logbookField: "Counter1", logbookFieldDescription: "Counter 1", sourceColumn: header, isRequired: false))
  }
  ```
  This preserves backwards compatibility with old Block-Time CSV exports while routing the data into the new multi-counter slot.

  FileImportService.swift lines 617–618: Delete the two lines:
  ```swift
  let customCountRaw = getValue("Custom Count")
  let customCount = Int(customCountRaw.trimmingCharacters(in: .whitespacesAndNewlines)) ?? 0
  ```
  The Counter1 path at lines 765–769 (`for i in 1...10 { let val = getValue("Counter\(i)")... flight.counterEntries[i] = val }`) handles the auto-mapped value.

  FileImportService.swift line 763: Remove the `customCount: isPositioning ? 0 : max(0, customCount)` line from the FlightSector init call. (The init parameter is removed in Task 6; this avoids a stale arg.) Leave the closing paren of the init properly placed — the line before is `scheduledArrival: scheduledArrival,` which becomes `scheduledArrival: scheduledArrival` (trailing comma removed) before the `)`.

  FileImportService.swift line 1274 (exportToCSV header constant): Remove the trailing `,Custom Count` from the header string. The constant must end at `...Remarks` before the for-loop appends counter headers.

  FileImportService.swift line 1318 (row array final element): Remove the `flight.customCount > 0 ? String(flight.customCount) : ""` element. The row must end at `escapeCSVField(flight.remarks)` before the for-loop appends counter values.

  Do NOT alter the `useLabelsAsHeaders` or `writeDefinitionsHeader` parameters or the `#DEFINITIONS:` prepend logic — backup path stays intact.
  </action>
  <verify>
    <automated>MISSING — Nelson builds locally. Manual verification: import a CSV containing a "Custom Count" column header → values land in Counter1. Export logbook CSV → header row contains no "Custom Count" column, only "Counter1…CounterN" trailing columns.</automated>
  </verify>
  <done>ImportMappingView.swift logbookFields does not contain "Custom Count" and the header loop maps "custom count" to Counter1. FileImportService.swift exportToCSV header does not contain "Custom Count" and the row builder does not reference `flight.customCount`. The FlightSector init call in FileImportService no longer passes `customCount:`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Switch spreadsheet and dashboard gates from logCustomCount to definitions.isEmpty</name>
  <files>Block-Time/Views/Screens/FrozenColumnSpreadsheetView.swift, Block-Time/Views/Screens/LogbookSpreadsheetView.swift, Block-Time/Views/Components/Dashboard/DashboardCardView.swift, Block-Time/Views/Components/Dashboard/DashboardEditSheet.swift, Block-Time/Views/Components/Dashboard/CustomCountCard.swift</files>
  <action>
  Invoke the `swiftui-pro` skill before editing.

  FrozenColumnSpreadsheetView.swift line 276 (`counterDefinitions` computed property): Replace the body with `CustomCounterService.shared.definitions`. Drop the `useCustomFields` local and the UserDefaults read.

  FrozenColumnSpreadsheetView.swift line 770 (inside `configure(flight:index:highlighted:config:)` on the cell): Replace the two lines `let useCustomFields = UserDefaults.standard.bool(forKey: "logCustomCount")` and `let definitions = useCustomFields ? CustomCounterService.shared.definitions : []` with `let definitions = CustomCounterService.shared.definitions`.

  LogbookSpreadsheetView.swift line 27 (`activeCounterCount` @State initializer): Replace the initial value expression with `CustomCounterService.shared.definitions.count`.

  LogbookSpreadsheetView.swift line 109 (inside `.onReceive(... UserDefaults.didChangeNotification)`): Replace the `count` computation with `CustomCounterService.shared.definitions.count` (drop the UserDefaults read). Keep the onReceive — `CustomCounterService` mutations may still post UserDefaults change notifications via its persistence path; keep the subscription so the UI refreshes.

  DashboardEditSheet.swift line 18: Delete `@AppStorage("logCustomCount") private var logCustomCount: Bool = false`.
  DashboardEditSheet.swift line 26 (`availablePool`): Replace `($0 != .customCount || logCustomCount)` with `($0 != .customCount || !CustomCounterService.shared.definitions.isEmpty)`.

  DashboardCardView.swift line 21: Delete `@AppStorage("logCustomCount") private var logCustomCount: Bool = false`.
  DashboardCardView.swift line 103: Replace `if logCustomCount { CustomCountCard() }` with `if !CustomCounterService.shared.definitions.isEmpty { CustomCountCard() }`. Keep `case .customCount:` and the enum case wiring.

  CustomCountCard.swift lines 96–97: Replace data extraction. Currently `let eligible = flights.filter { !$0.isPositioning && $0.customCount > 0 }` and `total = eligible.reduce(0) { $0 + $1.customCount }`. After this plan, FlightSector has no `customCount`. Change to:
  ```swift
  let eligible = flights.filter {
      !$0.isPositioning && (Int($0.counterEntries[1] ?? "") ?? 0) > 0
  }
  total = eligible.reduce(0) { $0 + (Int($1.counterEntries[1] ?? "") ?? 0) }
  ```
  Keep the `@AppStorage("customCountLabel") private var label` line — it's reading a persisted user label that still has value as a display string, and removing the AppStorage line would unbind the label display.
  </action>
  <verify>
    <automated>MISSING — Nelson builds locally. Manual verification: with at least one Custom Field defined, open the spreadsheet → counter columns visible; with zero definitions → counter columns hidden. Toggle nothing in Settings (no toggle remains) — counter visibility tracks definitions only. Open Dashboard editor → Custom Count card only appears in the available pool when definitions exist.</automated>
  </verify>
  <done>Neither spreadsheet view, neither dashboard view (DashboardCardView, DashboardEditSheet), nor CustomCountCard read `UserDefaults.standard.bool(forKey: "logCustomCount")` or `@AppStorage("logCustomCount")`. CustomCountCard reads from `counterEntries[1]` instead of `customCount`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Remove customCount @Published and read/write paths from FlightTimeExtractorViewModel</name>
  <files>Block-Time/ViewModels/FlightTimeExtractorViewModel.swift</files>
  <action>
  Invoke the `swiftui-pro` skill before editing.

  This ViewModel is still `ObservableObject` with `@Published` — per project memory `FlightTimeExtractorViewModel` is not migrating to @Observable. Maintain that pattern.

  Make these removals:

  1. Line 42 (inside `FlightDraft` struct): Delete `var customCount: Int = 0`.
  2. Line 161: Delete `@Published var customCount = 0`.
  3. Line 1632 (FlightSector init): Delete `customCount: isPositioning ? 0 : customCount,` argument.
  4. Line 1782 (sector→ViewModel loader): Delete `customCount = sector.customCount`.
  5. Line 1947 (FlightSector init): Delete `customCount: isPositioning ? 0 : customCount,` argument.
  6. Lines 2047 (change detection): Delete the `customCount != original.customCount ||` clause. Verify the surrounding `||` chain stays syntactically valid (remove the trailing `||` of the previous predicate or the leading `||` of this one, whichever keeps the expression compiling).
  7. Lines 2179–2180 (changes summary): Delete the `if customCount != original.customCount { changes.append("\(customCountLabel): \(original.customCount) → \(customCount)") }` block entirely.
  8. Line 2597 (reset): Delete `customCount = 0`.
  9. Line 2886 (FlightSector init): Delete `customCount: isPositioning ? 0 : customCount,` argument.
  10. Line 2942 (FlightSector init): Delete `customCount: isPositioning ? 0 : customCount,` argument.
  11. Line 3390 (FlightSector init for FlightDraft snapshot): Delete `customCount: customCount,` argument.
  12. Line 3456 (draft→VM loader): Delete `customCount = draft.customCount`.

  DO NOT touch:
  - `customCountLabel` property (line 188) — leave as is. It's still loaded from settings at lines 463 and 541–542 and used at line 858 (`customCountLabel = label`). These reads are harmless once `customCount` is gone; the label may be read by future code or Splash migration. Removing it would create unnecessary churn on the settings DTO consumer pattern.
  - `updateLogCustomCount(_:)` method (if present) — Settings was its only caller, but the migration path may invoke it indirectly. Search for `updateLogCustomCount` and if it has no callers after Task 1, leave the method body in place (do not delete public API in a cleanup pass). Confirm with grep that no other caller exists before deciding.
  - Any `logCustomCount` property accessor on the ViewModel — leave it; UserDefaultsService still persists the key.

  After edits, grep the file for `customCount` (case-sensitive, whole-word). Acceptable remaining matches: `customCountLabel` (property + assignments only). If `customCount` (without Label) still appears anywhere, address it.
  </action>
  <verify>
    <automated>MISSING — Nelson builds locally. After edit: `grep -n "\\bcustomCount\\b" Block-Time/ViewModels/FlightTimeExtractorViewModel.swift` must return zero matches (only `customCountLabel` is allowed).</automated>
  </verify>
  <done>FlightTimeExtractorViewModel.swift contains zero references to `customCount` as a standalone identifier. Only `customCountLabel` references remain. The 7 FlightSector init call sites (lines 1632, 1947, 2886, 2942, 3390 plus any others) no longer pass `customCount:`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Remove customCount writes from FlightDatabaseService and FlightSectorEditScreen</name>
  <files>Block-Time/Services/FlightDatabaseService.swift, Block-Time/Views/Screens/FlightSectorEditScreen.swift</files>
  <action>
  Invoke the `swiftui-pro` skill before editing.

  FlightDatabaseService.swift — surgical removals only:
  - Line 298: Delete `flight.customCount = Int16(sector.customCount)`.
  - Line 370: Delete `flight.customCount = Int16(sector.customCount)`.
  - Line 451: Delete `flight.customCount = Int16(updatedSector.customCount)`.
  - Line 1070: Delete `customCount: sector.customCount,` from the FlightSector init call inside the duplicate/copy path.
  - Line 1363: Delete `flight.customCount = Int16(sector.customCount)`.
  - Line 2203: Delete `customCount: Int(entity.customCount),` from the FlightSector init inside the entity→sector converter.

  DO NOT MODIFY:
  - Lines 1737–1759: `migrateLegacyCustomCounterToColumn1()` — this function reads `flight.customCount` via NSFetchRequest on FlightEntity and is the migration the whole cleanup depends on. It must remain bit-for-bit unchanged.
  - Any other reference to `entity.customCount` if it exists outside the converter (re-check with grep after edits).

  FlightSectorEditScreen.swift line 379: Delete `customCount: sector.customCount,` from the FlightSector init in the save path.

  After edits, run `grep -n "customCount" Block-Time/Services/FlightDatabaseService.swift` — the only acceptable remaining matches are inside `migrateLegacyCustomCounterToColumn1` (lines around 1737–1759). Anywhere else: investigate and remove.
  </action>
  <verify>
    <automated>MISSING — Nelson builds locally. After edit: every match of `customCount` in FlightDatabaseService.swift must fall inside the `migrateLegacyCustomCounterToColumn1` function body. FlightSectorEditScreen.swift must have zero matches.</automated>
  </verify>
  <done>FlightDatabaseService.swift: only the migration function references `customCount` (entity-side). FlightSectorEditScreen.swift: zero references. No write path assigns `flight.customCount` from a sector.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Remove customCount from FlightSector struct and fold v1 migration into Counter1</name>
  <files>Block-Time/Models/FlightLogbook.swift, Block-Time/Services/MigrationImportService.swift</files>
  <action>
  Invoke the `swiftui-pro` skill before editing.

  This task is sequenced LAST because all consumers (Tasks 1–5) must already be free of `sector.customCount` before the property goes away.

  FlightLogbook.swift:
  - Line 104: Delete `var customCount: Int`.
  - Line 115 (CodingKeys enum): Remove `customCount,` from the `case outTime, inTime, scheduledDeparture, scheduledArrival, customCount, counterEntries, createdAt` line. Result: `case outTime, inTime, scheduledDeparture, scheduledArrival, counterEntries, createdAt`.
  - Line 137 (init signature): Remove `customCount: Int = 0,` parameter. Keep `counterEntries: [Int: String] = [:]` and `createdAt: Date? = nil` in place.
  - Line 173 (init body): Delete `self.customCount = max(0, customCount)`.
  - Line 225 (decoder): Delete `customCount = try c.decode(Int.self, forKey: .customCount)`.
  - Line 335 (`init(from entity:)`): Delete `customCount: Int(entity.customCount),` argument.

  Backup-restore safety: FlightSector is Codable, but actual backups go through CSV (FileImportService), not JSON encoding of FlightSector directly. Verify by grepping for `JSONEncoder().*FlightSector` or `JSONDecoder().*FlightSector` across the project — expect zero hits. If any hit exists, surface it and stop before deleting the CodingKey.

  MigrationImportService.swift line 479: Currently:
  ```swift
  customCount: migrationFlight.customCount ?? 0
  ```
  Replace the entire `customCount:` argument with a counterEntries fold so v1 backup data is preserved into Counter1:
  ```swift
  counterEntries: {
      guard let cc = migrationFlight.customCount, cc > 0 else { return [:] }
      return [1: String(cc)]
  }()
  ```
  Keep the `MigrationFlight.customCount: Int?` DTO field (line 63) unchanged — v1 backup JSON still contains this key and decoding must continue to succeed.

  After all edits, run:
  ```
  grep -rn "\\bcustomCount\\b" Block-Time --include="*.swift" | grep -v customCountLabel
  ```
  Acceptable remaining matches:
  - `FlightEntity.customCount` (Core Data attribute access) inside FlightDatabaseService.migrateLegacyCustomCounterToColumn1 only.
  - `MigrationFlight.customCount` DTO field declaration in MigrationImportService.swift line 63 and the closure that reads `migrationFlight.customCount`.
  - `SplashScreenView.swift` references inside the legacy migration block (lines 91–180) — untouched.
  - `RawDatabaseView.swift` line 178 (`rawRow("customCount", String(flight.customCount))`) — this displays the raw Core Data attribute for diagnostics. Leave it.
  - `DashboardCardID.customCount` enum case references (legacy dashboard card ID).
  - `CustomCountCard.swift` — already updated in Task 3 to read counterEntries[1], so should have zero matches.

  Any other match: investigate and remove.
  </action>
  <verify>
    <automated>MISSING — Nelson builds locally. Acceptance: `grep -rn "\\bcustomCount\\b" Block-Time --include="*.swift" | grep -v customCountLabel | grep -v "DashboardCardID.customCount\\|case .customCount\\|case \"customCount\"\\|case customCount\\|\\.customCount[, ]\\|customCount\\.fill"` shows only entries inside `migrateLegacyCustomCounterToColumn1`, `MigrationImportService` DTO/closure, `SplashScreenView` migration block, and `RawDatabaseView` diagnostic row.</automated>
  </verify>
  <done>FlightSector struct no longer declares `customCount`. All non-migration, non-diagnostic, non-DashboardCardID call sites are gone. MigrationImportService preserves v1 customCount values by folding them into counterEntries[1]. Build (Nelson runs locally) reports zero `customCount` errors outside the allowed scopes.</done>
</task>

</tasks>

<verification>
After all six tasks complete:

1. Grep audit (run from project root):
   ```
   grep -rn "\\blogCustomCount\\b" Block-Time --include="*.swift"
   ```
   Expected matches only in: `UserDefaultsService.swift`, `MigrationImportService.swift`, `CloudKitSettingsSyncService.swift`, `SplashScreenView.swift` (migration block).

   ```
   grep -rn "\\bcustomCount\\b" Block-Time --include="*.swift" | grep -v customCountLabel | grep -v "DashboardCardID\\|case .customCount\\|customCounter"
   ```
   Expected matches only in: `FlightDatabaseService.migrateLegacyCustomCounterToColumn1`, `MigrationImportService` DTO + read closure, `SplashScreenView` migration block, `FlightEntity` Core Data attribute file, `RawDatabaseView.swift`.

2. Nelson builds locally and reports compiler status.

3. Manual smoke test (Nelson):
   - Settings → no "Use Custom Fields" toggle; Custom Fields section visible directly.
   - Add Flight with one Custom Field defined → counter row appears.
   - Add Flight with zero Custom Fields → no counter rows.
   - Import an old Block-Time CSV with a "Custom Count" column → values land in Counter1.
   - Export CSV → header has Counter1…CounterN, no "Custom Count".
   - Spreadsheet view shows counter columns iff definitions exist.
   - Dashboard editor offers CustomCountCard iff definitions exist.
</verification>

<success_criteria>
- Zero user-facing UI references to the legacy single `customCount`/`customCountLabel` field (Settings + Add/Edit + Dashboard editor are gated only on definitions).
- Zero `sector.customCount` reads or writes anywhere in the codebase.
- `FlightSector` struct compiles without a `customCount` property.
- v1 backup restores via MigrationImportService preserve customCount values into Counter1 (no data loss).
- Existing on-disk FlightEntity.customCount values continue to be readable; first-launch migration (migrateLegacyCustomCounterToColumn1) still copies them into counter1.
- Regular CSV export has no "Custom Count" column; backup-format CSV (via existing path) is unaffected.
- ImportMappingView auto-detects "Custom Count" headers in legacy CSVs and maps them to Counter1.
- UserDefaultsService logCustomCount and customCountLabel keys and persistence layer untouched.
- Nelson's local build passes (Nelson reports).
</success_criteria>

<output>
After completion, append a one-line summary to `.planning/STATE.md` under "Quick Tasks Completed" following the existing table format, and create `.planning/quick/260522-ke8-retire-legacy-customcount-field-across-a/260522-ke8-SUMMARY.md` capturing:
- Files touched per task.
- Any compiler issues encountered and how resolved.
- Confirmation that migration code paths are unchanged.
- Confirmation that no UserDefaults persistence keys were removed.
</output>
