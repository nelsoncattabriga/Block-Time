---
phase: quick-260601-sx6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Block-Time/Services/FlightDatabaseService.swift
  - Block-Time/Views/Screens/FlightsView.swift
  - Block-Time/Views/Screens/FlightsSplitView.swift
  - Block-Time/ViewModels/FlightTimeExtractorViewModel.swift
autonomous: true
requirements: [SX6-01]
must_haves:
  truths:
    - "Undo bar shows a human-readable description of the last undoable action (e.g. 'Deleted SYD-MEL · 14 May')"
    - "When no description is available, the bar falls back to the existing 'N changes to undo' text"
    - "Description stack stays in sync with undoableChangeCount across every CRUD path and undo"
  artifacts:
    - path: "Block-Time/Services/FlightDatabaseService.swift"
      provides: "undoDescriptions stack, lastUndoDescription, actionDescription params, fallback generation, pop on undo"
    - path: "Block-Time/Views/Screens/FlightsView.swift"
      provides: "undo bar primary label uses lastUndoDescription with fallback"
    - path: "Block-Time/Views/Screens/FlightsSplitView.swift"
      provides: "undo bar primary label uses lastUndoDescription with fallback"
    - path: "Block-Time/ViewModels/FlightTimeExtractorViewModel.swift"
      provides: "call sites pass a description string to save/update/delete"
  key_links:
    - from: "FlightDatabaseService CRUD methods"
      to: "undoDescriptions stack"
      via: "push description alongside undoableChangeCount += 1"
      pattern: "undoDescriptions.append"
    - from: "undoLastChange()"
      to: "undoDescriptions stack"
      via: "removeLast when non-empty after successful undo"
      pattern: "undoDescriptions.*removeLast"
    - from: "undo bar Text"
      to: "FlightDatabaseService.shared.lastUndoDescription"
      via: "refreshUndoState copies into @State, label reads it with fallback"
      pattern: "lastUndoDescription"
---

<objective>
Replace the undo bar's "N changes to undo" primary label with a human-readable description of the last undoable action (e.g. "Deleted SYD-MEL · 14 May", "Edited SYD-MEL"). When no description exists, fall back to the current count text.

Purpose: Give the pilot context about what an Undo tap will reverse, instead of an opaque count.
Output: A description stack on FlightDatabaseService kept in lockstep with undoableChangeCount, optional description params on the CRUD methods, call sites passing descriptions, and both undo bars showing the description.
</objective>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Confirmed from codebase — use directly, no exploration needed. -->

FlightSector (Block-Time/Models/FlightLogbook.swift) relevant fields, all String:
  var date: String          // "dd/MM/yyyy"
  var flightNumber: String
  var fromAirport: String
  var toAirport: String

FlightDatabaseService (Block-Time/Services/FlightDatabaseService.swift) existing undo infra:
  private(set) var undoableChangeCount: Int = 0
  var canUndo: Bool / var canRedo: Bool
  func undoLastChange() -> Bool   // on success: undoableChangeCount = max(0, undoableChangeCount - 1)
  func redoLastChange() -> Bool   // on success: undoableChangeCount += 1  (NO description tracking)

CRUD methods that currently do `undoableChangeCount += 1` (each must gain an optional actionDescription):
  func saveFlight(_ sector: FlightSector) -> Bool                                   // line ~293, increments at ~368
  func updateFlight(_ sector: FlightSector) -> Bool                                 // line ~382, increments at ~442
  func updateScheduledFlightWithActualData(_ scheduledFlight: FlightEntity, actualData: FlightSector) -> Bool  // ~646, increments ~738
  func deleteFlight(_ sector: FlightSector) -> Bool                                 // ~1024, increments ~1042
  func deleteFlights(_ sectors: [FlightSector]) -> Bool                             // ~1055, increments ~1069
  func deleteImportSession(_ sessionID: UUID) -> Int                               // ~1509, increments ~1524

NOTE: saveFlight is also called internally by duplicateFlights(...) — that call must keep compiling (param is optional, default nil → auto-fallback "Added ...").

Undo bar primary label (identical in both views) currently:
  Text("\(undoCount) \(undoCount == 1 ? "change" : "changes") to undo")
  - FlightsView.swift line ~258
  - FlightsSplitView.swift line ~566
refreshUndoState() in both reads undoableChangeCount + canRedo into @State.

ViewModel call sites (Block-Time/ViewModels/FlightTimeExtractorViewModel.swift):
  ~1620  databaseService.saveFlight(newFlight)
  ~1933  databaseService.updateFlight(updatedFlight)
  ~2239  databaseService.deleteFlight(tempSector)
  ~2870  databaseService.updateScheduledFlightWithActualData(scheduledFlight, actualData: actualFlightData)
  ~2926  databaseService.saveFlight(newFlight)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add description stack + optional params to FlightDatabaseService</name>
  <files>Block-Time/Services/FlightDatabaseService.swift</files>
  <action>
Invoke the swiftui-pro skill before editing.

1. In the "MARK: - Undo / Redo" section, directly under `undoableChangeCount`, add:
   - `private var undoDescriptions: [String] = []`
   - `var lastUndoDescription: String? { undoDescriptions.last }`
   These are accessed only on the main thread (viewContext is main-thread; all these CRUD methods run via viewContext.performAndWait on the main context). Keep them non-isolated stored properties on the existing class — do not introduce actor isolation.

2. Add a private fallback helper that builds a short description (under ~40 chars, no emoji) from a FlightSector and a verb:
   ```
   private func undoDescription(verb: String, for sector: FlightSector, includeDate: Bool) -> String {
       let route = "\(sector.fromAirport)-\(sector.toAirport)"
       if includeDate {
           let day = shortDay(from: sector.date)   // "14 May" from "dd/MM/yyyy", empty -> route only
           return day.isEmpty ? "\(verb) \(route)" : "\(verb) \(route) · \(day)"
       }
       return "\(verb) \(route)"
   }
   ```
   Add a `shortDay(from dd/MM/yyyy:)` helper that parses with the existing UTC `dateFormatter` and formats as "d MMM" (e.g. "14 May"); return "" on parse failure. Use a static "d MMM" formatter (do not reuse the "dd/MM/yyyy" one).

3. Add an `actionDescription: String? = nil` parameter to each method below. Where each currently does `undoableChangeCount += 1` on success, ALSO push the description so the two stay in lockstep. Resolve the pushed string as: `let desc = actionDescription ?? <auto-fallback>` then `undoDescriptions.append(desc)`.
   - `saveFlight(_:actionDescription:)` → fallback `undoDescription(verb: "Added", for: sector, includeDate: true)`
   - `updateFlight(_:actionDescription:)` → fallback `undoDescription(verb: "Edited", for: sector, includeDate: false)`
   - `updateScheduledFlightWithActualData(_:actualData:actionDescription:)` → fallback `undoDescription(verb: "Edited", for: actualData, includeDate: false)`
   - `deleteFlight(_:actionDescription:)` → fallback `undoDescription(verb: "Deleted", for: sector, includeDate: true)`
   - `deleteFlights(_:actionDescription:)` → fallback `"Deleted \(sectors.count) \(sectors.count == 1 ? "flight" : "flights")"`
   - `deleteImportSession(_:actionDescription:)` → fallback `"Deleted import batch"`

4. In `undoLastChange()`: after the existing `undoableChangeCount = max(0, undoableChangeCount - 1)` on success, add `if !undoDescriptions.isEmpty { undoDescriptions.removeLast() }`.

5. Do NOT touch `redoLastChange()` — redo keeps no description tracking per the task spec.

Keep every existing line, log, notification, and behaviour. Only ADD the param (with default nil), the two properties, the helpers, the append calls, and the pop. The internal `duplicateFlights` call to `saveFlight` keeps compiling because the param defaults to nil.
  </action>
  <verify>
    <automated>MISSING — no test target; Nelson builds locally. Verify by grep instead.</automated>
    grep -n "undoDescriptions" Block-Time/Services/FlightDatabaseService.swift shows: declaration, lastUndoDescription, 6 append sites, 1 removeLast in undoLastChange.
  </verify>
  <done>FlightDatabaseService exposes lastUndoDescription; all 6 CRUD methods accept actionDescription (default nil) and push a description in lockstep with undoableChangeCount; undoLastChange pops it; redoLastChange untouched. duplicateFlights still compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Show description in both undo bars and pass descriptions at call sites</name>
  <files>Block-Time/Views/Screens/FlightsView.swift, Block-Time/Views/Screens/FlightsSplitView.swift, Block-Time/ViewModels/FlightTimeExtractorViewModel.swift</files>
  <action>
Invoke the swiftui-pro skill before editing.

A. UNDO BAR LABEL — apply the SAME change in BOTH FlightsView.swift (~line 258) and FlightsSplitView.swift (~line 566). Both have an identical `refreshUndoState()` and undo bar.
   1. Add a new `@State private var undoDescription: String? = nil` next to the existing `undoCount`/`canRedo` state in each view.
   2. In each `refreshUndoState()`, add `undoDescription = FlightDatabaseService.shared.lastUndoDescription`.
   3. Replace the primary label line
      `Text("\(undoCount) \(undoCount == 1 ? "change" : "changes") to undo")`
      with
      `Text(undoDescription ?? "\(undoCount) \(undoCount == 1 ? "change" : "changes") to undo")`
      Keep the existing `.font(.subheadline.weight(.medium))` and `.foregroundColor(.primary)`. Add `.lineLimit(1).minimumScaleFactor(0.7)` on this Text so long descriptions never wrap or truncate (per project text-overflow rule). Leave the secondary "History clears when app closes" line unchanged.
   Do not change any other part of the undo bar (icon, Redo/Undo buttons, animations).

B. CALL SITES — pass a description at each FlightTimeExtractorViewModel call site (Block-Time/ViewModels/FlightTimeExtractorViewModel.swift). Build a short route string `"\(sector.fromAirport)-\(sector.toAirport)"` from the sector being saved/updated/deleted (the local `newFlight`/`updatedFlight`/`tempSector`/`actualFlightData` already in scope):
   1. ~1620 `saveFlight(newFlight)` → `saveFlight(newFlight, actionDescription: "Added \(newFlight.fromAirport)-\(newFlight.toAirport)")`
   2. ~1933 `updateFlight(updatedFlight)` → `updateFlight(updatedFlight, actionDescription: "Edited \(updatedFlight.fromAirport)-\(updatedFlight.toAirport)")`
   3. ~2239 `deleteFlight(tempSector)` → `deleteFlight(tempSector, actionDescription: "Deleted \(tempSector.fromAirport)-\(tempSector.toAirport)")`
   4. ~2870 `updateScheduledFlightWithActualData(scheduledFlight, actualData: actualFlightData)` → append `, actionDescription: "Edited \(actualFlightData.fromAirport)-\(actualFlightData.toAirport)")`
   5. ~2926 `saveFlight(newFlight)` → `saveFlight(newFlight, actionDescription: "Added \(newFlight.fromAirport)-\(newFlight.toAirport)")`
   Keep the surrounding `if databaseService...` / `let success = ...` structure exactly; only add the argument. Other paths (saveSummary, performDelete, performBulkDelete, deleteImportSession alert) keep calling without a description and rely on the auto-fallback — do not modify them.

Apply the FlightsView and FlightsSplitView label changes identically in both — per the project rule, missing one file causes an iPad/iPhone regression.
  </action>
  <verify>
    <automated>MISSING — no test target; verify by grep.</automated>
    grep -n "undoDescription ?? " Block-Time/Views/Screens/FlightsView.swift Block-Time/Views/Screens/FlightsSplitView.swift returns one hit each; grep -n "actionDescription:" Block-Time/ViewModels/FlightTimeExtractorViewModel.swift returns 5 hits.
  </verify>
  <done>Both undo bars show lastUndoDescription with the count fallback and lineLimit/minimumScaleFactor applied; 5 ViewModel call sites pass an actionDescription; no other call sites or undo-bar elements changed.</done>
</task>

</tasks>

<verification>
- grep confirms undoDescriptions stack with 6 appends + 1 removeLast in FlightDatabaseService; lastUndoDescription computed var present.
- Both undo bars read `undoDescription ?? "N changes to undo"`; identical in FlightsView and FlightsSplitView.
- 5 ViewModel call sites pass actionDescription; duplicateFlights/saveSummary/bulk paths unchanged and compiling via default nil.
- Nelson builds locally to confirm compilation and visual result.
</verification>

<success_criteria>
- Undo bar primary label reads e.g. "Deleted SYD-MEL · 14 May", "Edited SYD-MEL", or "Added SYD-MEL · 14 May".
- Bulk delete via the count fallback shows "Deleted N flights"; import batch shows "Deleted import batch".
- When undoDescriptions is empty (e.g. after redo or for untracked paths), the bar shows the original "N changes to undo".
- undoDescriptions never desyncs from undoableChangeCount (pushed and incremented together, popped and decremented together).
- No existing feature, button, or behaviour removed; no emoji; secondary label and Redo/Undo buttons unchanged.
</success_criteria>

<output>
After completion, create `.planning/quick/260601-sx6-add-human-readable-undo-descriptions-to-/260601-sx6-SUMMARY.md`
</output>
